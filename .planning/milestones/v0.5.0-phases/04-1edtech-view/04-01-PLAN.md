---
phase: 04-1edtech-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/edtech.js
  - src/data/riskLabels.js
  - src/views/ReportsView.vue
autonomous: true
requirements:
  - EDTECH-01
must_haves:
  truths:
    - "All 27 records in src/data/edtech.js have certificationStandard and certifiedDate fields"
    - "Certified and Expired vendors in edtech.js have non-null certificationStandard values drawn from real 1EdTech standards (OneRoster, CASE, Caliper, QTI, LTI, TDT)"
    - "Not Certified and In Review vendors in edtech.js have null certificationStandard and null certifiedDate"
    - "src/data/riskLabels.js exports EDTECH_STATUS_COLORS mapping all four status values to hex colors"
    - "src/views/ReportsView.vue tabs array contains only 'DPA' and 'Risk Position' (no '1EdTech')"
  artifacts:
    - path: "src/data/edtech.js"
      provides: "1EdTech certification records with status + standard + certified date for all 27 vendors"
      contains: "certificationStandard"
    - path: "src/data/riskLabels.js"
      provides: "EDTECH_STATUS_COLORS export for 1EdTech badge styling"
      exports: ["EDTECH_STATUS_COLORS"]
    - path: "src/views/ReportsView.vue"
      provides: "Tab bar with only DPA and Risk Position (1EdTech removed)"
      contains: "['DPA', 'Risk Position']"
  key_links:
    - from: "src/data/edtech.js"
      to: "src/components/VendorDrawer.vue (consumer in Plan 02)"
      via: "default export of array of 27 records keyed by vendorId, each with certificationStatus + certificationStandard + certifiedDate"
      pattern: "certificationStandard"
    - from: "src/data/riskLabels.js"
      to: "src/components/VendorDrawer.vue (consumer in Plan 02)"
      via: "named export EDTECH_STATUS_COLORS"
      pattern: "export const EDTECH_STATUS_COLORS"
---

<objective>
Prepare all non-VendorDrawer surface area for the 1EdTech feature: enrich the edtech.js data file with real 1EdTech standard names + certification dates, add the EDTECH_STATUS_COLORS constants export to riskLabels.js, and remove the dead '1EdTech' tab from ReportsView.

Purpose: All three changes are independent file edits with no inter-dependencies. Bundling them in one Wave 1 plan lets Plan 02 (VendorDrawer integration) consume both data sources cleanly in Wave 2.

Output: edtech.js with 27 enriched records, riskLabels.js with EDTECH_STATUS_COLORS export, ReportsView.vue with the 1EdTech tab removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1edtech-view/04-CONTEXT.md

<interfaces>
<!-- Existing riskLabels.js exports the executor will be adding alongside -->
<!-- src/data/riskLabels.js current content: -->

```javascript
export const RISK_LABELS = {
  HIGH_USAGE_NO_DPA: 'High Usage / No DPA',
  NO_DPA: 'No DPA',
  HIGH_RISK_SCORE: 'High Risk Score'
}

export const RISK_LABEL_COLORS = {
  'High Usage / No DPA': '#b91c1c',
  'No DPA': '#ef4444',
  'High Risk Score': '#d97706'
}

export const DPA_STATUS_COLORS = {
  Signed: '#16a34a',
  Expired: '#dc2626',
  Pending: '#f59e0b',
  Unsigned: '#6b7280'
}
```

<!-- src/data/edtech.js current shape (all 27 records follow this pattern): -->

```javascript
{
  vendorId: 'vendor-google-classroom',
  certificationStatus: 'Certified'
}
```

<!-- src/views/ReportsView.vue current tabs array (line 5): -->

```javascript
const tabs = ['DPA', 'Risk Position', '1EdTech']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich src/data/edtech.js with certificationStandard and certifiedDate fields for all 27 records</name>
  <files>src/data/edtech.js</files>
  <read_first>
    - src/data/edtech.js (the file being modified — see all 27 current records with their certificationStatus values)
    - .planning/phases/04-1edtech-view/04-CONTEXT.md (decisions D-04 through D-07 specify the exact field semantics)
  </read_first>
  <action>
    Modify src/data/edtech.js so every record gains two new fields: `certificationStandard` (string | null) and `certifiedDate` (string | null, ISO format YYYY-MM-DD).

    Rules per CONTEXT.md decisions D-05, D-06, D-07:
    - `Certified` vendors: certificationStandard = non-null standard name; certifiedDate = plausible date within last 1-4 years (2022-2025 range; today is 2026-05-13)
    - `Expired` vendors: certificationStandard = non-null standard name; certifiedDate = original certification date (older, e.g. 2019-2021)
    - `Not Certified` vendors: certificationStandard = null; certifiedDate = null
    - `In Review` vendors: certificationStandard = null; certifiedDate = null

    Use AT LEAST 3 different real 1EdTech standard names across Certified + Expired vendors. Standards to draw from: 'OneRoster 2.0', 'CASE 1.0', 'Caliper 1.2', 'QTI 3.0', 'LTI 1.3', 'TDT 1.0'.

    Apply this exact distribution (15 Certified, 6 Not Certified, 3 Expired, 4 In Review = 28 — note edtech.js currently has 27 records; preserve all 27 vendorIds):
    - vendor-google-classroom (Certified) → standard: 'OneRoster 2.0', date: '2024-03-15'
    - vendor-canvas (Certified) → standard: 'LTI 1.3', date: '2023-11-20'
    - vendor-schoology (Certified) → standard: 'OneRoster 2.0', date: '2024-01-10'
    - vendor-zoom (Not Certified) → standard: null, date: null
    - vendor-microsoft-teams (Certified) → standard: 'LTI 1.3', date: '2023-09-05'
    - vendor-clever (Certified) → standard: 'OneRoster 2.0', date: '2025-02-18'
    - vendor-classlink (Certified) → standard: 'OneRoster 2.0', date: '2024-08-22'
    - vendor-ixl (Certified) → standard: 'Caliper 1.2', date: '2024-05-30'
    - vendor-khan-academy (Certified) → standard: 'Caliper 1.2', date: '2025-01-08'
    - vendor-seesaw (Certified) → standard: 'LTI 1.3', date: '2024-04-12'
    - vendor-kahoot (In Review) → standard: null, date: null
    - vendor-quizlet (Not Certified) → standard: null, date: null
    - vendor-nearpod (Certified) → standard: 'LTI 1.3', date: '2024-07-19'
    - vendor-pear-deck (Certified) → standard: 'LTI 1.3', date: '2025-03-04'
    - vendor-flipgrid (Expired) → standard: 'LTI 1.3', date: '2020-06-15'
    - vendor-edpuzzle (In Review) → standard: null, date: null
    - vendor-newsela (Certified) → standard: 'CASE 1.0', date: '2024-10-25'
    - vendor-readworks (Not Certified) → standard: null, date: null
    - vendor-mystery-science (Certified) → standard: 'QTI 3.0', date: '2025-04-02'
    - vendor-prodigy (Not Certified) → standard: null, date: null
    - vendor-dreambox (In Review) → standard: null, date: null
    - vendor-renaissance (Expired) → standard: 'QTI 3.0', date: '2019-08-11'
    - vendor-naviance (Not Certified) → standard: null, date: null
    - vendor-powerschool (Expired) → standard: 'OneRoster 2.0', date: '2020-11-30'
    - vendor-infinite-campus (Not Certified) → standard: null, date: null
    - vendor-securly (In Review) → standard: null, date: null
    - vendor-gaggle (Not Certified) → standard: null, date: null

    Preserve the existing `vendorId` and `certificationStatus` values exactly — do not change them. Maintain the 2-space indentation and trailing comma style already used in the file. The file is `export default [...]` syntax — keep it.

    Standards used: OneRoster 2.0 (6 records), LTI 1.3 (6 records), Caliper 1.2 (2 records), CASE 1.0 (1 record), QTI 3.0 (2 records) — 5 distinct standards across 17 non-null records, satisfying D-07's "at least 3 different standards" requirement.
  </action>
  <verify>
    <automated>node -e "const e = require('./src/data/edtech.js').default; const ok = e.length === 27 && e.every(r => 'certificationStandard' in r && 'certifiedDate' in r) && e.filter(r => r.certificationStatus === 'Certified').every(r => r.certificationStandard !== null && r.certifiedDate !== null) && e.filter(r => r.certificationStatus === 'Not Certified' || r.certificationStatus === 'In Review').every(r => r.certificationStandard === null && r.certifiedDate === null) && new Set(e.filter(r => r.certificationStandard).map(r => r.certificationStandard)).size >= 3; if (!ok) { console.error('FAIL'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/edtech.js contains exactly 27 records (grep -c "vendorId:" src/data/edtech.js outputs 27)
    - Every record contains the literal string `certificationStandard:` (grep -c "certificationStandard:" outputs 27)
    - Every record contains the literal string `certifiedDate:` (grep -c "certifiedDate:" outputs 27)
    - File contains at least 3 different standard string values among: 'OneRoster 2.0', 'CASE 1.0', 'Caliper 1.2', 'QTI 3.0', 'LTI 1.3', 'TDT 1.0'
    - Every `'Not Certified'` and `'In Review'` record has both `certificationStandard: null` and `certifiedDate: null`
    - Every `'Certified'` and `'Expired'` record has a non-null `certificationStandard` string and a non-null `certifiedDate` ISO date string
    - The file still uses `export default [` syntax
    - All 27 original vendorId values are preserved (vendor-google-classroom through vendor-gaggle)
    - `npm run build` exits 0 (file is syntactically valid JS)
  </acceptance_criteria>
  <done>edtech.js has 27 enriched records with certificationStandard + certifiedDate, null for Not Certified/In Review, non-null for Certified/Expired, using at least 3 distinct real 1EdTech standard names.</done>
</task>

<task type="auto">
  <name>Task 2: Add EDTECH_STATUS_COLORS export to src/data/riskLabels.js</name>
  <files>src/data/riskLabels.js</files>
  <read_first>
    - src/data/riskLabels.js (the file being modified — see existing DPA_STATUS_COLORS export style to replicate)
    - .planning/phases/04-1edtech-view/04-CONTEXT.md (decision D-12 specifies the exact color values)
  </read_first>
  <action>
    Append a new named export `EDTECH_STATUS_COLORS` to the end of src/data/riskLabels.js. Use the same object-literal style as the existing `DPA_STATUS_COLORS` export (keys = status string, values = hex color string).

    Add this exact code at the end of the file (after the existing DPA_STATUS_COLORS export, with one blank line of separation):

    ```javascript

    // Status badge colors for 1EdTech certification status column (per D-12).
    // Reuses the DPA semantic palette for visual consistency across the portal.
    // - Certified     → green-600 (#16a34a)  — certified and current
    // - Not Certified → gray-500  (#6b7280)  — vendor has no certification
    // - In Review     → amber-500 (#f59e0b)  — application in progress
    // - Expired       → red-600   (#dc2626)  — previously certified, now lapsed
    export const EDTECH_STATUS_COLORS = {
      Certified: '#16a34a',
      'Not Certified': '#6b7280',
      'In Review': '#f59e0b',
      Expired: '#dc2626'
    }
    ```

    Preserve all existing exports (RISK_LABELS, RISK_LABEL_COLORS, DPA_STATUS_COLORS) unchanged. Do not modify their values or comments.
  </action>
  <verify>
    <automated>node -e "const r = require('./src/data/riskLabels.js'); const c = r.EDTECH_STATUS_COLORS; const ok = c && c.Certified === '#16a34a' && c['Not Certified'] === '#6b7280' && c['In Review'] === '#f59e0b' && c.Expired === '#dc2626' && r.DPA_STATUS_COLORS && r.RISK_LABEL_COLORS && r.RISK_LABELS; if (!ok) { console.error('FAIL'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/riskLabels.js contains the literal string `export const EDTECH_STATUS_COLORS = {`
    - The export object has keys: Certified, 'Not Certified', 'In Review', Expired
    - Color values exactly match: Certified=#16a34a, 'Not Certified'=#6b7280, 'In Review'=#f59e0b, Expired=#dc2626
    - Existing exports DPA_STATUS_COLORS, RISK_LABEL_COLORS, RISK_LABELS still present and unchanged (grep finds each export literal)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>riskLabels.js has the new EDTECH_STATUS_COLORS export with the four exact status→hex mappings from D-12, and all prior exports remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Remove '1EdTech' from ReportsView.vue tabs array</name>
  <files>src/views/ReportsView.vue</files>
  <read_first>
    - src/views/ReportsView.vue (the file being modified — see current tabs array at line 5)
    - .planning/phases/04-1edtech-view/04-CONTEXT.md (decision D-03 specifies the exact change)
  </read_first>
  <action>
    In src/views/ReportsView.vue, change the `tabs` array declaration from:

    ```javascript
    const tabs = ['DPA', 'Risk Position', '1EdTech']
    ```

    to:

    ```javascript
    const tabs = ['DPA', 'Risk Position']
    ```

    That is the ONLY change. Do not modify the `activeTab` ref, the template, the skeletons, or any other code. The DPA and Risk Position tabs remain as skeleton stubs — that is intentional (per D-03).
  </action>
  <verify>
    <automated>grep -q "const tabs = \['DPA', 'Risk Position'\]" src/views/ReportsView.vue && ! grep -q "1EdTech" src/views/ReportsView.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/views/ReportsView.vue contains the exact literal string `const tabs = ['DPA', 'Risk Position']`
    - src/views/ReportsView.vue does NOT contain the string `1EdTech` anywhere in the file
    - The rest of the file is unchanged (`activeTab` ref, template, skeletons all preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ReportsView.vue tabs array has exactly two entries ('DPA', 'Risk Position'), the '1EdTech' string is gone, and no other content changed.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `npm run build` succeeds (no syntax errors introduced)
- `grep -c "vendorId:" src/data/edtech.js` returns 27
- `grep -c "certificationStandard:" src/data/edtech.js` returns 27
- `grep -c "certifiedDate:" src/data/edtech.js` returns 27
- `grep "EDTECH_STATUS_COLORS" src/data/riskLabels.js` finds the new export
- `grep "1EdTech" src/views/ReportsView.vue` returns nothing
</verification>

<success_criteria>
Plan 01 is done when:
1. edtech.js has 27 records, each with certificationStandard + certifiedDate fields following D-05/D-06/D-07 rules
2. riskLabels.js exports EDTECH_STATUS_COLORS with the four exact mappings from D-12
3. ReportsView.vue tabs array is `['DPA', 'Risk Position']` and the file contains no '1EdTech' string anywhere
4. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-1edtech-view/04-01-SUMMARY.md` documenting:
- Which standards were assigned to which vendor categories (recap of the distribution)
- Confirmation that EDTECH_STATUS_COLORS keys exactly match the four certificationStatus values used in edtech.js
- Files modified and line counts
</output>
