---
phase: 02-data-layer-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/data/vendors.js
  - src/data/discovery.js
  - src/data/dpa.js
  - src/data/edtech.js
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "Four data files exist under src/data/ and export non-empty arrays"
    - "Every vendor row in discovery.js, dpa.js, edtech.js has a vendorId that matches a row in vendors.js"
    - "Every vendor in vendors.js has all 10 privacy score dimensions populated with integers 1-10"
    - "echarts and vue-echarts are installed and present in package.json dependencies"
  artifacts:
    - path: "src/data/vendors.js"
      provides: "Vendor base metadata + privacyScore object (10 dimensions)"
      min_lines: 200
    - path: "src/data/discovery.js"
      provides: "Usage metrics per vendor (frequency, lastSeen, userCount, studentCount)"
      min_lines: 100
    - path: "src/data/dpa.js"
      provides: "DPA status placeholder (status, signedDate, expiryDate)"
      min_lines: 80
    - path: "src/data/edtech.js"
      provides: "1EdTech certification status placeholder"
      min_lines: 60
    - path: "package.json"
      provides: "echarts and vue-echarts in dependencies"
      contains: "echarts"
  key_links:
    - from: "src/data/discovery.js"
      to: "src/data/vendors.js"
      via: "shared vendorId string"
      pattern: "vendor-[a-z0-9-]+"
    - from: "src/data/dpa.js"
      to: "src/data/vendors.js"
      via: "shared vendorId string"
      pattern: "vendor-[a-z0-9-]+"
    - from: "src/data/edtech.js"
      to: "src/data/vendors.js"
      via: "shared vendorId string"
      pattern: "vendor-[a-z0-9-]+"
---

<objective>
Establish the complete static data layer for the District Demo Portal. Install ECharts dependencies (used by VendorDrawer in Plan 03 and the risk donut chart in Phase 5), then create the four `src/data/*.js` fixture files with 27 realistic edtech vendors keyed by a stable `vendorId`.

Purpose: Phase 2 and every downstream phase consumes these files. The data shape and vendorId join key must be stable from day one so that the DPA view (Phase 3), 1EdTech view (Phase 4), and Risk Position view (Phase 5) can build their UIs without revisiting fixtures.

Output: Four ES module data files, package.json updated with echarts + vue-echarts, npm run build still exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-discovery/02-CONTEXT.md
@.planning/phases/02-data-layer-discovery/02-RESEARCH.md
@.planning/phases/02-data-layer-discovery/02-UI-SPEC.md

<interfaces>
<!-- Data shape contracts that Plan 03 (VendorDrawer) and Plan 04 (DiscoveryView) depend on. -->
<!-- These shapes are LOCKED — D-02 through D-08 in CONTEXT.md. -->

// src/data/vendors.js — Array<Vendor>
{
  vendorId: 'vendor-google-classroom',   // stable join key (D-06)
  name: 'Google Classroom',
  category: 'LMS',
  privacyScore: {                        // D-07 — all 10 keys required, each int 1-10
    informationCollected: 7,
    useOfInformation: 6,
    dataSharing: 5,
    securityMeasures: 9,
    userRights: 7,
    retentionPeriod: 6,
    complianceWithLaws: 8,
    updatesToPolicy: 7,
    clarityAndTransparency: 8,
    contactInformation: 9
  }
}

// src/data/discovery.js — Array<DiscoveryRecord>
{
  vendorId: 'vendor-google-classroom',
  frequency: 'Daily',           // 'Daily' | 'Weekly' | 'Monthly' | 'Rarely'
  lastSeen: '2026-05-12',       // ISO date string
  userCount: 1842,              // integer
  studentCount: 14230           // integer
}

// src/data/dpa.js — Array<DpaRecord> (placeholder for Phase 3 view)
{
  vendorId: 'vendor-google-classroom',
  status: 'Signed',             // 'Signed' | 'Unsigned' | 'Expired' | 'Pending'
  signedDate: '2024-08-15',     // ISO date or null when not signed
  expiryDate: '2026-08-15'      // ISO date or null
}

// src/data/edtech.js — Array<EdtechRecord> (placeholder for Phase 4 view)
{
  vendorId: 'vendor-google-classroom',
  certificationStatus: 'Certified'  // 'Certified' | 'In Review' | 'Not Certified' | 'Expired'
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install echarts and vue-echarts dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (verify current dependency list, do not duplicate)
    - .planning/phases/02-data-layer-discovery/02-RESEARCH.md (version table — Standard Stack section)
  </read_first>
  <action>
    Run from the repo root:

    ```
    npm install echarts vue-echarts
    ```

    Target versions (from RESEARCH.md, confirmed against npm registry 2026-05-13):
    - `echarts` → `^6.0.0`
    - `vue-echarts` → `^8.0.1`

    Both should be added to `dependencies` (not devDependencies) — they are runtime imports used by `src/main.js` and `src/components/VendorDrawer.vue`.

    After install, run `npm run build` to confirm the dependency graph still resolves with the new packages.

    Do NOT register VChart or call `use([...])` in this task — that wiring happens in Plan 02 alongside the tags store restructure.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"echarts": "^6.0.0"` (or 6.x latest) in `dependencies`
    - `package.json` contains `"vue-echarts": "^8.0.1"` (or 8.x latest) in `dependencies`
    - `package-lock.json` has been updated (modified date is newer than before)
    - `npm run build` exits 0
    - `node_modules/echarts/package.json` exists
    - `node_modules/vue-echarts/package.json` exists
  </acceptance_criteria>
  <done>echarts + vue-echarts installed, build still green, ready for Plan 02 to register VChart globally.</done>
</task>

<task type="auto">
  <name>Task 2: Create vendors.js with 27 vendors and full privacy scores</name>
  <files>src/data/vendors.js</files>
  <read_first>
    - .planning/phases/02-data-layer-discovery/02-CONTEXT.md (D-02, D-06, D-07, D-08, D-10, D-12)
    - .planning/phases/02-data-layer-discovery/02-RESEARCH.md (Pattern 1 — Static ES Module Data Files, Code Examples section)
    - src/data/vendors.js (verify path — file should not yet exist)
  </read_first>
  <action>
    Create `src/data/vendors.js` as a plain ES module. The file must `export default` an array of exactly 27 vendor objects. Each vendor object MUST have this exact shape (D-02, D-07):

    ```javascript
    {
      vendorId: 'vendor-<kebab-case-name>',    // stable join key (D-06)
      name: '<display name>',
      category: '<category string>',
      privacyScore: {
        informationCollected: <int 1-10>,
        useOfInformation: <int 1-10>,
        dataSharing: <int 1-10>,
        securityMeasures: <int 1-10>,
        userRights: <int 1-10>,
        retentionPeriod: <int 1-10>,
        complianceWithLaws: <int 1-10>,
        updatesToPolicy: <int 1-10>,
        clarityAndTransparency: <int 1-10>,
        contactInformation: <int 1-10>
      }
    }
    ```

    Use these 27 vendors (real edtech brands — D-10). vendorId column is the EXACT string to use:

    | vendorId | name | category |
    |----------|------|----------|
    | vendor-google-classroom | Google Classroom | LMS |
    | vendor-canvas | Canvas | LMS |
    | vendor-schoology | Schoology | LMS |
    | vendor-zoom | Zoom | Video Conferencing |
    | vendor-microsoft-teams | Microsoft Teams | Communication |
    | vendor-clever | Clever | SSO / Identity |
    | vendor-classlink | ClassLink | SSO / Identity |
    | vendor-ixl | IXL | Adaptive Learning |
    | vendor-khan-academy | Khan Academy | Adaptive Learning |
    | vendor-seesaw | Seesaw | Communication |
    | vendor-kahoot | Kahoot! | Assessment |
    | vendor-quizlet | Quizlet | Study Tools |
    | vendor-nearpod | Nearpod | Engagement |
    | vendor-pear-deck | Pear Deck | Engagement |
    | vendor-flipgrid | Flip | Engagement |
    | vendor-edpuzzle | Edpuzzle | Video Lessons |
    | vendor-newsela | Newsela | Content / ELA |
    | vendor-readworks | ReadWorks | Content / ELA |
    | vendor-mystery-science | Mystery Science | Content / Science |
    | vendor-prodigy | Prodigy | Adaptive Learning |
    | vendor-dreambox | DreamBox | Adaptive Learning |
    | vendor-renaissance | Renaissance | Assessment |
    | vendor-naviance | Naviance | Counseling |
    | vendor-powerschool | PowerSchool | SIS |
    | vendor-infinite-campus | Infinite Campus | SIS |
    | vendor-securly | Securly | Web Filtering |
    | vendor-gaggle | Gaggle | Safety / Monitoring |

    Privacy score distribution rule (D-12 — plausible variation):
    - At least 5 vendors must have at least one dimension scored 1, 2, or 3 (creates High risk candidates for Phase 5)
    - At least 5 vendors must have all 10 dimensions scored 8, 9, or 10 (creates Low risk candidates)
    - The remaining vendors should mix scores between 4-8 with no obvious patterns
    - Every dimension value MUST be an integer between 1 and 10 inclusive — no decimals, no nulls, no missing keys

    Do NOT include a `totalScore` field — D-08 specifies total score is COMPUTED at render time, never stored.

    Indent 2 spaces. End the array with `]` and the file with `\n`. Default export, no named exports.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/vendors.js` exists
    - File contains `export default` at the top level
    - Grep `vendorId:` in `src/data/vendors.js` returns exactly 27 matches
    - Grep `informationCollected:` returns exactly 27 matches (one per vendor)
    - Grep `contactInformation:` returns exactly 27 matches (last privacy dimension, confirms all 10 keys per object)
    - Grep `totalScore` returns 0 matches (D-08 — computed, never stored)
    - File contains `vendor-google-classroom`
    - File contains `vendor-clever`
    - File contains `vendor-powerschool`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>vendors.js exports 27 vendor records, every vendor has all 10 privacy dimensions, build green.</done>
</task>

<task type="auto">
  <name>Task 3: Create discovery.js, dpa.js, edtech.js with matching vendorIds</name>
  <files>src/data/discovery.js, src/data/dpa.js, src/data/edtech.js</files>
  <read_first>
    - src/data/vendors.js (the file you just created — extract the 27 vendorIds to use here)
    - .planning/phases/02-data-layer-discovery/02-CONTEXT.md (D-03, D-04, D-05, D-11)
    - .planning/phases/02-data-layer-discovery/02-RESEARCH.md (Pattern 1, Code Examples — discovery.js sample)
  </read_first>
  <action>
    Create three sibling data files. Each `export default` an array of 27 records, one per vendor from vendors.js, joined by exact `vendorId` string match.

    **File 1 — `src/data/discovery.js`** (D-03):

    Shape per record:
    ```javascript
    {
      vendorId: 'vendor-google-classroom',
      frequency: 'Daily',           // 'Daily' | 'Weekly' | 'Monthly' | 'Rarely'
      lastSeen: '2026-05-12',       // ISO date string YYYY-MM-DD
      userCount: 1842,              // integer
      studentCount: 14230           // integer
    }
    ```

    Distribution rule (D-11):
    - ~12 vendors → `frequency: 'Daily'`, userCount 800-2500, studentCount 8000-18000 (high usage)
    - ~9 vendors → `frequency: 'Weekly'`, userCount 200-800, studentCount 2000-8000 (medium usage)
    - ~4 vendors → `frequency: 'Monthly'`, userCount 50-200, studentCount 400-2000 (low usage)
    - ~2 vendors → `frequency: 'Rarely'`, userCount 5-50, studentCount 30-400 (very low usage)

    `lastSeen` values must be valid ISO dates between `2026-01-01` and `2026-05-13` (today). Daily-frequency vendors should have lastSeen within the last 7 days (2026-05-06 to 2026-05-13). Rarely-frequency vendors can be as old as January 2026.

    **File 2 — `src/data/dpa.js`** (D-04):

    Shape per record:
    ```javascript
    {
      vendorId: 'vendor-google-classroom',
      status: 'Signed',             // 'Signed' | 'Unsigned' | 'Expired' | 'Pending'
      signedDate: '2024-08-15',     // ISO date or null when not signed
      expiryDate: '2026-08-15'      // ISO date or null
    }
    ```

    Distribution rule (D-11):
    - ~15 vendors → `status: 'Signed'`, signedDate 2023-2025, expiryDate 2026-2027 (future)
    - ~5 vendors → `status: 'Expired'`, signedDate 2022-2023, expiryDate 2024-2025 (past)
    - ~5 vendors → `status: 'Unsigned'`, signedDate `null`, expiryDate `null`
    - ~2 vendors → `status: 'Pending'`, signedDate `null`, expiryDate `null`

    The "Unsigned" + "Expired" rows are what Phase 3's "needs attention" headline count will surface (DPA-02). The high-usage Daily vendors should preferentially get Signed status (realistic — districts sign DPAs for the tools they use most), and a couple of low-privacy-score Daily vendors should be Unsigned or Expired (creates High risk candidates for Phase 5).

    **File 3 — `src/data/edtech.js`** (D-05):

    Shape per record:
    ```javascript
    {
      vendorId: 'vendor-google-classroom',
      certificationStatus: 'Certified'  // 'Certified' | 'In Review' | 'Not Certified' | 'Expired'
    }
    ```

    Distribution rule:
    - ~12 vendors → `certificationStatus: 'Certified'`
    - ~4 vendors → `certificationStatus: 'In Review'`
    - ~8 vendors → `certificationStatus: 'Not Certified'`
    - ~3 vendors → `certificationStatus: 'Expired'`

    **All three files:**
    - Each file must include all 27 vendorIds from vendors.js — no missing, no extras, no typos
    - 2-space indent, default export only, file ends with `\n`
    - No commented-out vendors, no TODO placeholders

    Hand-verify after writing: open `src/data/vendors.js` and confirm the same 27 vendorId strings appear in each of the three new files.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/discovery.js` exists; grep `vendorId:` returns exactly 27 matches
    - `src/data/dpa.js` exists; grep `vendorId:` returns exactly 27 matches
    - `src/data/edtech.js` exists; grep `vendorId:` returns exactly 27 matches
    - Grep `frequency: 'Daily'` in discovery.js returns 10-14 matches (target ~12)
    - Grep `status: 'Signed'` in dpa.js returns 13-17 matches (target ~15)
    - Grep `status: 'Unsigned'` in dpa.js returns 3-7 matches (target ~5)
    - Grep `certificationStatus: 'Certified'` in edtech.js returns 10-14 matches (target ~12)
    - `vendor-google-classroom` appears in all 3 files (and in vendors.js — total 4 occurrences across `src/data/`)
    - `vendor-powerschool` appears in all 3 files
    - All three files contain `export default`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All four data files seeded, vendorIds consistent across all four files, build green.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `npm run build` exits 0 (no broken imports, no syntax errors)
2. Inspect `src/data/vendors.js` — 27 vendor records, each with full privacyScore object
3. Inspect `src/data/discovery.js`, `dpa.js`, `edtech.js` — 27 records each, vendorIds align with vendors.js
4. Spot-check 3 vendorIds (google-classroom, clever, powerschool) appear in all four files exactly once each
</verification>

<success_criteria>
- Phase 2 data layer is fully seeded — no further fixture work required in Phases 3-5
- Every downstream view can `import` from `src/data/<file>.js` with a stable contract
- The vendorId join key is consistent across all four files
- `echarts` and `vue-echarts` are installed and ready for Plan 02 to register
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-discovery/02-01-SUMMARY.md` capturing:
- Vendor count and category distribution
- Frequency / DPA status / certification status distributions actually produced
- Confirmation that vendorIds align across all four files
- Confirmation that echarts + vue-echarts are installed (with installed versions)
</output>
