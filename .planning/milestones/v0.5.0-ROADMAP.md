# Milestone v0.5.0: MVP

**Status:** ✅ SHIPPED 2026-05-21
**Phases:** 1–6
**Total Plans:** 15

## Overview

Full demo portal for edtech sales — displays school district data across Discovery, DPA, Risk Position, and 1EdTech sections. Built with Vue 3 + Vite + PrimeVue 4 + Tailwind v4 as a static SPA on GitHub Pages, with all data in editable `src/data/*.js` files for same-day sales iteration.

## Phases

### Phase 1: Shell & Routing

**Goal**: The app has a working SPA skeleton — all routes resolve, the persistent sidebar nav is visible on every page, and the Pinia store is wired up for tags state
**Depends on**: Nothing (first phase)
**Requirements**: FOUND-01, FOUND-02, FOUND-04
**Plans**: 2 plans

Plans:
- [x] 01-01: Install deps and wire foundation (router + Pinia + PrimeVue + Tailwind v4)
- [x] 01-02: Build AppShell, SidebarNav, and 4 stub views with skeletons

**Details:**
- Vue Router 4 with `createWebHashHistory('/district-demo/')` for GitHub Pages compatibility
- Pinia `useTagsStore` initialized and accessible from all components
- PrimeVue 4 with custom SchoolDay Aura preset (`#484ce6` primary, dark mode disabled)
- Tailwind v4 `@theme` brand color tokens; AppShell with dark sidebar (`bg-sidebar #111827`)
- SidebarNav driven by `router.getRoutes().filter(r => r.meta?.nav)` — adding routes auto-updates sidebar
- Completed: 2026-05-13

### Phase 2: Data Layer + Discovery

**Goal**: All mock data schemas are established in `src/data/*.js` and the Discovery page is fully functional — vendors are browsable, sortable, filterable, drillable, and taggable
**Depends on**: Phase 1
**Requirements**: FOUND-03, DISC-01, DISC-02, DISC-03, TAGS-02
**Plans**: 4 plans

Plans:
- [x] 02-01: Install ECharts + seed 4 data files (vendors, discovery, dpa, edtech) with 27 vendors
- [x] 02-02: Restructure tags store with tagGroups + localStorage; register VChart globally
- [x] 02-03: Build VendorDrawer with usage detail, 10-axis radar chart, and grouped tag MultiSelect
- [x] 02-04: Replace DiscoveryView skeleton with sortable/filterable table mounting the drawer

**Details:**
- 27-vendor DataTable (sortable, filterable) — name, category, usage metrics, user/student counts
- VendorDrawer slide-over with 10-axis ECharts radar chart and grouped tag MultiSelect
- Tag assignment via MultiSelect; assignments persisted to localStorage via `watch()`
- `vendorId` stable join key (`vendor-<kebab-case>`) across all 4 `src/data/*.js` files
- `FilterMatchMode` from `@primevue/core/api` (PrimeVue 4 path)
- Completed: 2026-05-13

### Phase 3: DPA View

**Goal**: District admins can review every vendor's DPA status at a glance, with the most pressing items surfaced in the page header
**Depends on**: Phase 2
**Requirements**: DPA-01, DPA-02
**Plans**: 3 plans

Plans:
- [x] 03-01: Add riskLabel field to dpa.js (27 records) + create shared riskLabels.js constants module
- [x] 03-02: Build DpaGrid.vue component + add Discovery|DPA tab bar to DiscoveryView
- [x] 03-03: Extend VendorDrawer with DPA section + replace Dashboard skeleton with Top 8 At-Risk card

**Details:**
- DpaGrid: 6-column sortable table with color-coded status badges (Signed/Unsigned/Expired/Pending)
- Discovery|DPA tab bar in DiscoveryView for navigation
- VendorDrawer extended with DPA section (status, dates, risk label)
- Dashboard replaced with Top 8 Vendors Needing Attention card + KPI tiles
- `riskLabels.js` constants module for shared badge color maps
- Completed: 2026-05-13

### Phase 4: 1EdTech View

**Goal**: District admins can see each vendor's 1EdTech certification status, standard, and certified date inside the VendorDrawer — no standalone 1EdTech page; the drawer is the single 1EdTech surface
**Depends on**: Phase 2
**Requirements**: EDTECH-01
**Plans**: 2 plans

Plans:
- [x] 04-01: Enrich edtech.js with certificationStandard + certifiedDate; add EDTECH_STATUS_COLORS to riskLabels.js; remove '1EdTech' from ReportsView tabs
- [x] 04-02: Add 1EdTech section to VendorDrawer.vue (between DPA and Privacy Policy Score) with status badge + conditional standard/date rows + empty fallback

**Details:**
- `edtech.js` enriched with `certificationStandard` + `certifiedDate` (27 records, 5 real standards: QTI 3.0, LTI Advantage, OneRoster 1.1, Caliper 1.1, CASE 1.0)
- `EDTECH_STATUS_COLORS` added to `riskLabels.js`
- VendorDrawer 1EdTech section between DPA and Privacy Score
- Graceful empty state: "No 1EdTech record on file."
- Dead '1EdTech' tab removed from ReportsView
- Completed: 2026-05-13

### Phase 5: Risk Position View

**Goal**: District admins can see a converged picture of vendor risk — a summary chart and per-vendor risk tiers derived from DPA status and usage volume
**Depends on**: Phase 3, Phase 4
**Requirements**: RISK-01, RISK-02
**Plans**: 2 plans

Plans:
- [x] 05-01: Add RISK_TIER_COLORS to riskLabels.js + register PieChart and LegendComponent in main.js
- [x] 05-02: Rewrite ReportsView.vue as the Risk Position page (donut chart + sortable 5-column vendor table + VendorDrawer drill-down)

**Details:**
- ReportsView rewritten as full Risk Position page
- ECharts donut chart showing High:2 / Medium:7 / Low:18 tier distribution
- Computed `calcTier` formula: DPA status + usage volume + 1EdTech certification modifier
- Sortable 5-column vendor table (name, category, DPA status, usage, tier badge)
- VendorDrawer drill-down on row click
- DPA tab skeleton removed
- Completed: 2026-05-13

### Phase 6: Tags Management

**Goal**: Users can manage the full tag library — create new tags, rename existing ones, change colors, and delete tags — from a dedicated settings page
**Depends on**: Phase 2
**Requirements**: TAGS-01
**Plans**: 2 plans

Plans:
- [x] 06-01: Export SEED_TAG_GROUPS from src/stores/tags.js so SettingsView can deep-clone it for reset-to-defaults
- [x] 06-02: Rewrite SettingsView.vue as the Tags management page (inline CRUD, 8-swatch palette, PrimeVue Dialog confirms, reset-to-defaults)

**Details:**
- SettingsView rewritten as full Tags CRUD page
- Inline group/tag rename (click-to-edit)
- 8-swatch preset color palette for tag color assignment
- PrimeVue Dialog confirms for delete and reset-to-defaults
- Cascade delete: removing a tag cleans all vendor assignments
- Reset-to-defaults restores SEED_TAG_GROUPS deep-clone
- Completed: 2026-05-21

---

## Milestone Summary

**Key Decisions:**

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| `createWebHashHistory` | GitHub Pages cannot serve HTML5 pushstate fallbacks | ✓ Good |
| Mock data in `src/data/*.js` | Same-day iteration requires data decoupled from components | ✓ Good |
| `vendorId` join key | Stable cross-file join without brittle display-name matching | ✓ Good |
| `darkModeSelector: false` | Portal uses structural dark sidebar, not system dark mode | ✓ Good |
| Risk visualization: donut + table | Donut shows distribution; table enables per-vendor drill-down | ✓ Good |
| 1EdTech in VendorDrawer only | No standalone page — drawer is the single surface | ✓ Good |
| Vite 8 stub views early | Rolldown resolves lazy imports at build time; stubs required in Plan 01 | ✓ Good |
| `@primeuix/themes` import path | Correct PrimeVue 4.x path (not legacy `@primevue/themes` alias) | ✓ Good |

**Issues Resolved:**

- Vite 8 (rolldown) build-time lazy import resolution — required stub views in Phase 1 Plan 01
- `FilterMatchMode` import path change in PrimeVue 4 (`@primevue/core/api`)
- Phase 3 progress row in ROADMAP.md stale (showed 1/3 but all plans complete)

**Issues Deferred:**

- v2 requirements: DPA version currency, expiry warnings, Risk contributing factors, 1EdTech filter/cross-reference, Tag filter in Discovery, tag persistence cross-refresh

**Technical Debt Incurred:**

- None significant — all v1 requirements shipped cleanly
- `src/components/HelloWorld.vue` left on disk (unused, harmless)

---

*Archived: 2026-05-21*
*See .planning/MILESTONES.md for summary index*
