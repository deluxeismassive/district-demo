---
phase: 08-layout-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app.vue
  - app/types/page-meta.d.ts
  - app/pages/index.vue
  - app/pages/discovery.vue
  - app/pages/dpa.vue
  - app/pages/risk.vue
  - app/pages/tags.vue
  - app/layouts/default.vue
autonomous: true
requirements:
  - NUXT-04
  - LAYOUT-01

must_haves:
  truths:
    - "Navigating to /, /discovery, /dpa, /risk, /tags each loads its correct stub page (HTTP 200, page-specific h1 content) — no 404s"
    - "The dark sidebar nav is visible on every page (Schoolday header + 5 NuxtLink items + Lakewood Unified School District header bar) with active route highlighted via NuxtLink's exact-active-class"
    - "No router/index.ts, router.config.ts, or createRouter/createWebHistory import exists anywhere in app/, src/, or nuxt.config.ts — all routing is driven by app/pages/ file structure"
    - "definePageMeta provides nav label and icon metadata consumed by the sidebar via useRouter().options.routes filtered by meta.nav === true and sorted by meta.navOrder — no hardcoded route names in default.vue"
  artifacts:
    - path: "app/app.vue"
      provides: "UApp -> NuxtLayout -> NuxtPage chain (replaces Phase 7 placeholder)"
      contains: "<UApp>, <NuxtLayout>, <NuxtPage"
    - path: "app/types/page-meta.d.ts"
      provides: "PageMeta interface augmentation with nav, navLabel, navIcon, navOrder optional fields"
      contains: "declare module '#app', interface PageMeta, export {}"
    - path: "app/pages/index.vue"
      provides: "Dashboard stub at route /; nav: true, navLabel: Dashboard, navIcon: i-lucide-home, navOrder: 10"
      contains: "definePageMeta, navLabel: 'Dashboard', i-lucide-home, <h1"
    - path: "app/pages/discovery.vue"
      provides: "Discovery stub at route /discovery; nav: true, navLabel: Discovery, navIcon: i-lucide-search, navOrder: 20"
      contains: "definePageMeta, navLabel: 'Discovery', i-lucide-search, <h1"
    - path: "app/pages/dpa.vue"
      provides: "DPA stub at route /dpa; nav: true, navLabel: DPA, navIcon: i-lucide-file-text, navOrder: 30"
      contains: "definePageMeta, navLabel: 'DPA', i-lucide-file-text, <h1"
    - path: "app/pages/risk.vue"
      provides: "Risk Position stub at route /risk; nav: true, navLabel: Risk Position, navIcon: i-lucide-alert-triangle, navOrder: 40"
      contains: "definePageMeta, navLabel: 'Risk Position', i-lucide-alert-triangle, <h1"
    - path: "app/pages/tags.vue"
      provides: "Tags Management stub at route /tags; nav: true, navLabel: Tags, navIcon: i-lucide-tag, navOrder: 50"
      contains: "definePageMeta, navLabel: 'Tags', i-lucide-tag, <h1"
    - path: "app/layouts/default.vue"
      provides: "Persistent app shell: dark sidebar with NuxtLink list driven by useRouter().options.routes + Lakewood header bar + <slot/> for page content"
      contains: "useRouter, router.options.routes, meta.nav === true, navOrder, <NuxtLink, exact-active-class, bg-[var(--color-sidebar)], Schoolday, Lakewood Unified School District, <slot"
  key_links:
    - from: "app/app.vue"
      to: "app/layouts/default.vue"
      via: "<NuxtLayout> wrapper in app.vue template auto-mounts the default layout"
      pattern: "<NuxtLayout>"
    - from: "app/layouts/default.vue"
      to: "app/pages/*.vue (all 5 stubs)"
      via: "useRouter().options.routes filtered by meta.nav === true; rendered as NuxtLink list"
      pattern: "router\\.options\\.routes.*meta\\?\\.nav"
    - from: "app/pages/*.vue"
      to: "sidebar nav rendering"
      via: "definePageMeta({ nav: true, navLabel, navIcon, navOrder }) baked into route meta at build time"
      pattern: "definePageMeta\\("
    - from: "app/layouts/default.vue <slot />"
      to: "current route's page component"
      via: "<NuxtPage /> in app.vue → <NuxtLayout> default slot → page mounts in <main>'s <slot />"
      pattern: "<slot"
    - from: "app/types/page-meta.d.ts"
      to: "definePageMeta type safety across all pages"
      via: "declare module '#app' { interface PageMeta { nav?, navLabel?, navIcon?, navOrder? } }"
      pattern: "declare module '#app'"
---

<objective>
Phase 8 delivers the persistent app shell and file-based routing for all 5 demo sections. By the end of this plan:

- `app/app.vue` becomes the canonical `<UApp><NuxtLayout><NuxtPage /></NuxtLayout></UApp>` triple (the Phase 7 placeholder paragraph is gone).
- A single `app/layouts/default.vue` renders the dark sidebar + white header + `<slot />` content area on every route.
- The sidebar nav iterates `useRouter().options.routes` filtered by `meta.nav === true` and sorted by `meta.navOrder` — no hardcoded route names.
- Each of `app/pages/index.vue`, `discovery.vue`, `dpa.vue`, `risk.vue`, `tags.vue` is a minimal stub (h1 + one-line placeholder) carrying `definePageMeta` with `nav`, `navLabel`, `navIcon`, `navOrder`.
- `app/types/page-meta.d.ts` augments the `PageMeta` interface so the custom keys autocomplete and typecheck.

Purpose: Closes requirements NUXT-04 (file-based routing, no manual router) and LAYOUT-01 (persistent shell with dark sidebar + NuxtLink active highlighting). Provides the routing skeleton that Phases 9-12 will hang real content on.

Output: 7 new files + 1 modified file (app/app.vue). No npm installs. No deletions. No data fetching. No charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-layout-routing/08-RESEARCH.md
@.planning/phases/08-layout-routing/08-VALIDATION.md
@.planning/phases/07-nuxt-scaffold/07-02-SUMMARY.md
@app/app.vue
@app/app.config.ts
@app/assets/css/main.css
@nuxt.config.ts
@CLAUDE.md
</context>

<interfaces>
<!-- All snippets below are copied verbatim from 08-RESEARCH.md § Code Examples. -->
<!-- Executors MUST use these exact contents; no codebase exploration needed. -->

### app/app.vue — final contents (REPLACES the Phase 7 stub)

```vue
<template>
  <UApp>
    <NuxtLayout>
      <NuxtPage />
    </NuxtLayout>
  </UApp>
</template>
```

(No `<script>` block. No imports. UApp/NuxtLayout/NuxtPage are auto-registered by Nuxt + @nuxt/ui.)

### app/types/page-meta.d.ts — PageMeta interface augmentation

```ts
// Source: nuxt.com/docs/4.x/directory-structure/app/pages#typing-custom-metadata
declare module '#app' {
  interface PageMeta {
    nav?: boolean
    navLabel?: string
    navIcon?: string
    navOrder?: number
  }
}

// Required: must have at least one top-level import or export
// to be treated as a module (not a script file).
export {}
```

### app/pages/index.vue — Dashboard stub

```vue
<script setup lang="ts">
definePageMeta({
  nav: true,
  navLabel: 'Dashboard',
  navIcon: 'i-lucide-home',
  navOrder: 10,
})
</script>

<template>
  <div class="p-8">
    <h1 class="text-2xl font-semibold text-gray-900">Dashboard</h1>
    <p class="text-gray-600 mt-2">District overview. (Phase 11 wires the Top 8 card.)</p>
  </div>
</template>
```

### app/pages/discovery.vue — Discovery stub

```vue
<script setup lang="ts">
definePageMeta({
  nav: true,
  navLabel: 'Discovery',
  navIcon: 'i-lucide-search',
  navOrder: 20,
})
</script>

<template>
  <div class="p-8">
    <h1 class="text-2xl font-semibold text-gray-900">Discovery</h1>
    <p class="text-gray-600 mt-2">App and domain usage across the district. (Phase 10 wires data.)</p>
  </div>
</template>
```

### app/pages/dpa.vue — DPA stub

```vue
<script setup lang="ts">
definePageMeta({
  nav: true,
  navLabel: 'DPA',
  navIcon: 'i-lucide-file-text',
  navOrder: 30,
})
</script>

<template>
  <div class="p-8">
    <h1 class="text-2xl font-semibold text-gray-900">DPA</h1>
    <p class="text-gray-600 mt-2">Data Privacy Agreement status by vendor. (Phase 11 wires table + badges.)</p>
  </div>
</template>
```

### app/pages/risk.vue — Risk Position stub

```vue
<script setup lang="ts">
definePageMeta({
  nav: true,
  navLabel: 'Risk Position',
  navIcon: 'i-lucide-alert-triangle',
  navOrder: 40,
})
</script>

<template>
  <div class="p-8">
    <h1 class="text-2xl font-semibold text-gray-900">Risk Position</h1>
    <p class="text-gray-600 mt-2">Vendor risk distribution and tier breakdown. (Phase 12 wires donut + table.)</p>
  </div>
</template>
```

### app/pages/tags.vue — Tags Management stub

```vue
<script setup lang="ts">
definePageMeta({
  nav: true,
  navLabel: 'Tags',
  navIcon: 'i-lucide-tag',
  navOrder: 50,
})
</script>

<template>
  <div class="p-8">
    <h1 class="text-2xl font-semibold text-gray-900">Tags</h1>
    <p class="text-gray-600 mt-2">Tag groups, child tags, and vendor assignments. (Phase 12 wires CRUD.)</p>
  </div>
</template>
```

### app/layouts/default.vue — full shell (sidebar + header + slot)

```vue
<!-- Source: composite from v0.5.0 SidebarNav + AppShell (git commit 15d021c~1), upgraded to NuxtLink + UIcon -->
<script setup lang="ts">
const router = useRouter()

const navRoutes = computed(() =>
  router.options.routes
    .filter((r) => r.meta?.nav === true)
    .sort((a, b) => (a.meta?.navOrder ?? 999) - (b.meta?.navOrder ?? 999))
)
</script>

<template>
  <div class="flex h-screen overflow-hidden">
    <aside class="flex flex-col w-64 h-screen bg-[var(--color-sidebar)] text-white shrink-0">
      <div class="px-6 py-6 border-b border-gray-700">
        <span class="text-lg font-semibold tracking-wide">Schoolday</span>
      </div>

      <ul class="flex-1 py-4 px-3 space-y-1">
        <li v-for="route in navRoutes" :key="String(route.path)">
          <NuxtLink
            :to="route.path"
            class="flex items-center gap-3 px-3 py-2 rounded-lg text-gray-400 hover:bg-gray-800 hover:text-white transition-colors text-sm font-medium"
            active-class="!bg-primary-600 !text-white"
            exact-active-class="!bg-primary-600 !text-white"
          >
            <UIcon
              :name="(route.meta?.navIcon as string) ?? 'i-lucide-circle'"
              class="text-base shrink-0"
            />
            <span>{{ route.meta?.navLabel ?? route.name }}</span>
          </NuxtLink>
        </li>
      </ul>
    </aside>

    <div class="flex flex-col flex-1 overflow-hidden">
      <header class="flex items-center h-14 bg-white border-b border-gray-200 pl-6 shrink-0">
        <span class="text-sm text-gray-700">Lakewood Unified School District</span>
      </header>
      <main class="flex-1 overflow-y-auto bg-gray-50">
        <slot />
      </main>
    </div>
  </div>
</template>
```

### Exact color and class reference (from 08-RESEARCH.md § 6)

| Element | Tailwind class | Resolved color |
|---------|----------------|----------------|
| Sidebar background | `bg-[var(--color-sidebar)]` | `#1f2128` (sidebar var; v0.5.0 used `#111827`, Phase 7's main.css declares the var) |
| Sidebar divider | `border-gray-700` | `#374151` |
| Brand text | `text-white` | `#ffffff` |
| Inactive link text | `text-gray-400` | `#9ca3af` |
| Inactive link hover bg | `hover:bg-gray-800` | `#1f2937` |
| Active link bg | `!bg-primary-600` | `#3c40cc` (= brand-600 from main.css) |
| Active link text | `!text-white` | `#ffffff` |
| Header bar bg | `bg-white` | `#ffffff` |
| Header bottom border | `border-gray-200` | `#e5e7eb` |
| Main content bg | `bg-gray-50` | `#f9fafb` |

NOTE: The exact `--color-sidebar` value declared in `app/assets/css/main.css` is `#111827` (read at line 17). The brand palette `--color-brand-600` is `#3c40cc` (line 12). Do NOT redeclare these.

### Anti-patterns (from 08-RESEARCH.md § 9) — DO NOT introduce

- DO NOT create `app/router/index.ts`, `router.config.ts`, or any explicit router config.
- DO NOT `import { createRouter, createWebHistory, createWebHashHistory }`.
- DO NOT hardcode a `NAV_ROUTES = [...]` const in the sidebar — iterate `useRouter().options.routes`.
- DO NOT use `<RouterLink>` — use `<NuxtLink>`.
- DO NOT omit `<NuxtLayout>` from `app/app.vue` — `default.vue` would never mount.
- DO NOT use `bg-sidebar` utility — Tailwind v4 may not generate it from a single non-graded var. Use `bg-[var(--color-sidebar)]` explicitly.
- DO NOT set `definePageMeta({ layout: 'something' })` on any stub — they must use the default.
- DO NOT use `definePageMeta` with imported constants, template strings with vars, or computed values — the macro extracts literals at build time only.
- DO NOT split the sidebar into a separate `app/components/SidebarNav.vue` — inline it in `default.vue` for Phase 8.
- DO NOT add page transitions, keepalive, or middleware on Phase 8 stubs.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PageMeta type augmentation and replace app/app.vue with NuxtLayout chain</name>
  <files>app/types/page-meta.d.ts, app/app.vue</files>
  <read_first>
    - app/app.vue (current Phase 7 stub — must be REPLACED, not appended to)
    - .planning/phases/08-layout-routing/08-RESEARCH.md § 2 (definePageMeta + type augmentation), § 3 (app.vue final shape), § 8 Pitfalls 7, 9, 10 (NuxtLayout required; export {} required; correct app.vue path)
    - .planning/phases/08-layout-routing/08-VALIDATION.md § Per-Task Verification Map
    - nuxt.config.ts (must NOT be edited — confirm modules and ssr config remain intact)
  </read_first>
  <action>
    Create `app/types/page-meta.d.ts` with the EXACT content from the `<interfaces>` block above (`declare module '#app' { interface PageMeta { nav?: boolean; navLabel?: string; navIcon?: string; navOrder?: number } }` followed by `export {}`).

    Then REPLACE the entire contents of `app/app.vue` (currently the Phase 7 paragraph stub at lines 1-8) with the exact 7-line block from the `<interfaces>` § "app/app.vue — final contents":

    ```vue
    <template>
      <UApp>
        <NuxtLayout>
          <NuxtPage />
        </NuxtLayout>
      </UApp>
    </template>
    ```

    No `<script>` block. No imports. Two spaces indentation per CLAUDE.md.

    After both files are written, run `npx nuxi prepare` to regenerate `.nuxt/types/` so the new PageMeta augmentation is picked up by `nuxi typecheck`.

    DO NOT edit nuxt.config.ts, app.config.ts, or app/assets/css/main.css. The sidebar var (`--color-sidebar`) and brand palette (`--color-brand-*`) are already declared.

    DO NOT create any router file. DO NOT introduce `vue-router` as an explicit dep.
  </action>
  <verify>
    <automated>
      test -f app/types/page-meta.d.ts &amp;&amp; \
      grep -q "declare module '#app'" app/types/page-meta.d.ts &amp;&amp; \
      grep -q 'interface PageMeta' app/types/page-meta.d.ts &amp;&amp; \
      grep -q 'navLabel?: string' app/types/page-meta.d.ts &amp;&amp; \
      grep -q 'navIcon?: string' app/types/page-meta.d.ts &amp;&amp; \
      grep -q 'navOrder?: number' app/types/page-meta.d.ts &amp;&amp; \
      grep -q '^export {}' app/types/page-meta.d.ts &amp;&amp; \
      grep -q '&lt;UApp&gt;' app/app.vue &amp;&amp; \
      grep -q '&lt;NuxtLayout&gt;' app/app.vue &amp;&amp; \
      grep -q '&lt;NuxtPage' app/app.vue &amp;&amp; \
      ! grep -q 'District Demo — Nuxt 4 scaffold' app/app.vue &amp;&amp; \
      npx nuxi prepare &amp;&amp; \
      npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/types/page-meta.d.ts` exits 0
    - `grep -q "declare module '#app'" app/types/page-meta.d.ts` matches
    - `grep -q 'interface PageMeta' app/types/page-meta.d.ts` matches
    - `grep -q '^export {}' app/types/page-meta.d.ts` matches (last-line export — required for the file to be treated as a module per Pitfall 9)
    - `grep -q '<UApp>' app/app.vue` matches
    - `grep -q '<NuxtLayout>' app/app.vue` matches
    - `grep -q '<NuxtPage' app/app.vue` matches
    - `! grep -q 'District Demo — Nuxt 4 scaffold' app/app.vue` (Phase 7 placeholder paragraph removed)
    - `! grep -q '<script' app/app.vue` (no script block in app.vue)
    - `npm run typecheck` exits 0
    - No `app/router/` directory or `router.config.ts` exists anywhere (`! find app/ src/ -maxdepth 3 -name 'router*.ts' -o -name 'router*.js' 2>/dev/null | grep .`)
  </acceptance_criteria>
  <done>app/app.vue is the canonical UApp/NuxtLayout/NuxtPage triple and app/types/page-meta.d.ts augments PageMeta with the 4 nav keys; typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create all 5 page stubs under app/pages/ with definePageMeta nav metadata</name>
  <files>app/pages/index.vue, app/pages/discovery.vue, app/pages/dpa.vue, app/pages/risk.vue, app/pages/tags.vue</files>
  <read_first>
    - .planning/phases/08-layout-routing/08-RESEARCH.md § Code Examples (5 page stub snippets — copy verbatim), § 7 (icon mapping and navOrder rationale), § 8 Pitfalls 1, 2, 4, 5, 13 (filename casing; definePageMeta literal-only; icon name typos; no layout override)
    - .planning/phases/08-layout-routing/08-VALIDATION.md § Phase Requirements → Test Map (every page must SSR-render its h1 marker)
    - CLAUDE.md naming conventions (lowercase page filenames, 2-space indent)
  </read_first>
  <action>
    Create the following 5 page files under `app/pages/` with the EXACT content from the `<interfaces>` block above. Filenames are LOWERCASE (Pitfall 4). No subdirectories. No leading underscores.

    **app/pages/index.vue** — copy `<interfaces>` § "app/pages/index.vue — Dashboard stub" verbatim:
    - `definePageMeta({ nav: true, navLabel: 'Dashboard', navIcon: 'i-lucide-home', navOrder: 10 })`
    - Template: `<div class="p-8">`, `<h1 class="text-2xl font-semibold text-gray-900">Dashboard</h1>`, `<p class="text-gray-600 mt-2">District overview. (Phase 11 wires the Top 8 card.)</p>`

    **app/pages/discovery.vue** — copy `<interfaces>` § "app/pages/discovery.vue — Discovery stub" verbatim:
    - `definePageMeta({ nav: true, navLabel: 'Discovery', navIcon: 'i-lucide-search', navOrder: 20 })`
    - h1 text: `Discovery`
    - p text: `App and domain usage across the district. (Phase 10 wires data.)`

    **app/pages/dpa.vue** — copy `<interfaces>` § "app/pages/dpa.vue — DPA stub" verbatim:
    - `definePageMeta({ nav: true, navLabel: 'DPA', navIcon: 'i-lucide-file-text', navOrder: 30 })`
    - h1 text: `DPA`
    - p text: `Data Privacy Agreement status by vendor. (Phase 11 wires table + badges.)`

    **app/pages/risk.vue** — copy `<interfaces>` § "app/pages/risk.vue — Risk Position stub" verbatim:
    - `definePageMeta({ nav: true, navLabel: 'Risk Position', navIcon: 'i-lucide-alert-triangle', navOrder: 40 })`
    - h1 text: `Risk Position`
    - p text: `Vendor risk distribution and tier breakdown. (Phase 12 wires donut + table.)`

    **app/pages/tags.vue** — copy `<interfaces>` § "app/pages/tags.vue — Tags Management stub" verbatim:
    - `definePageMeta({ nav: true, navLabel: 'Tags', navIcon: 'i-lucide-tag', navOrder: 50 })`
    - h1 text: `Tags`
    - p text: `Tag groups, child tags, and vendor assignments. (Phase 12 wires CRUD.)`

    All five files MUST use `<script setup lang="ts">` (TypeScript). All definePageMeta values MUST be string/number/boolean literals — no imported constants, no template strings with vars (Pitfall 2). No `layout:` key (Pitfall 13). No `keepalive`, no `pageTransition`, no `middleware` (out of Phase 8 scope).

    After all 5 files are written, run `npx nuxi prepare` to regenerate `.nuxt/types/` so the new route names are added to the typed router.
  </action>
  <verify>
    <automated>
      for p in index discovery dpa risk tags; do
        test -f "app/pages/$p.vue" || { echo "MISSING: app/pages/$p.vue"; exit 1; };
        grep -q 'definePageMeta(' "app/pages/$p.vue" || { echo "no definePageMeta in $p.vue"; exit 1; };
        grep -q 'nav: true' "app/pages/$p.vue" || { echo "no nav:true in $p.vue"; exit 1; };
        grep -q 'navLabel:' "app/pages/$p.vue" || { echo "no navLabel in $p.vue"; exit 1; };
        grep -q 'navIcon:' "app/pages/$p.vue" || { echo "no navIcon in $p.vue"; exit 1; };
        grep -q 'navOrder:' "app/pages/$p.vue" || { echo "no navOrder in $p.vue"; exit 1; };
        grep -q '&lt;h1' "app/pages/$p.vue" || { echo "no &lt;h1 in $p.vue"; exit 1; };
        grep -q 'lang="ts"' "app/pages/$p.vue" || { echo "no lang=ts in $p.vue"; exit 1; };
      done
      grep -q "navLabel: 'Dashboard'" app/pages/index.vue &amp;&amp; \
      grep -q "i-lucide-home" app/pages/index.vue &amp;&amp; \
      grep -q "navLabel: 'Discovery'" app/pages/discovery.vue &amp;&amp; \
      grep -q "i-lucide-search" app/pages/discovery.vue &amp;&amp; \
      grep -q "navLabel: 'DPA'" app/pages/dpa.vue &amp;&amp; \
      grep -q "i-lucide-file-text" app/pages/dpa.vue &amp;&amp; \
      grep -q "navLabel: 'Risk Position'" app/pages/risk.vue &amp;&amp; \
      grep -q "i-lucide-alert-triangle" app/pages/risk.vue &amp;&amp; \
      grep -q "navLabel: 'Tags'" app/pages/tags.vue &amp;&amp; \
      grep -q "i-lucide-tag" app/pages/tags.vue &amp;&amp; \
      npx nuxi prepare &amp;&amp; \
      npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/pages/index.vue` AND `test -f app/pages/discovery.vue` AND `test -f app/pages/dpa.vue` AND `test -f app/pages/risk.vue` AND `test -f app/pages/tags.vue` ALL exit 0
    - All 5 page files contain `definePageMeta(`
    - All 5 page files contain `nav: true` (literal boolean per Pitfall 2)
    - Each page has its specific navLabel literal: Dashboard / Discovery / DPA / Risk Position / Tags
    - Each page has its specific navIcon literal: i-lucide-home / i-lucide-search / i-lucide-file-text / i-lucide-alert-triangle / i-lucide-tag (Pitfall 5 — exact spelling)
    - All 5 pages use `<script setup lang="ts">`
    - No page sets `layout:` or `keepalive:` or `pageTransition:` (Pitfall 13)
    - `! grep -q 'definePageMeta({ layout' app/pages/*.vue`
    - `! grep -rE "createRouter|createWebHistory|createWebHashHistory" app/ 2>/dev/null` (still no router config introduced)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>All 5 page stubs exist with correct definePageMeta literals; routes /, /discovery, /dpa, /risk, /tags are registered in Nuxt's route table; typecheck remains green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/layouts/default.vue — dark sidebar shell with NuxtLink list driven by router.options.routes</name>
  <files>app/layouts/default.vue</files>
  <read_first>
    - .planning/phases/08-layout-routing/08-RESEARCH.md § 3 (full layout structure), § 4 (NuxtLink active-class behavior + Tailwind v4 caveat), § 6 (verbatim v0.5.0 colors and Tailwind classes), § Code Examples "app/layouts/default.vue — full shell" (copy verbatim)
    - .planning/phases/08-layout-routing/08-RESEARCH.md § 8 Pitfalls 3, 6, 8, 11, 12, 15 (exact-active-class; bg-[var()] not bg-sidebar; filter meta.nav; root link active prefix; slot placement; no separate Sidebar component)
    - .planning/phases/08-layout-routing/08-VALIDATION.md § Phase Requirements → Test Map (LAYOUT-01 grep probes)
    - app/assets/css/main.css (confirm `--color-sidebar` and `--color-brand-600` are declared; do NOT redeclare)
    - app/pages/index.vue (must exist so router.options.routes has entries to filter)
  </read_first>
  <action>
    Create `app/layouts/default.vue` with the EXACT content from the `<interfaces>` § "app/layouts/default.vue — full shell" block. Concrete required pieces (all verbatim from research):

    1. `<script setup lang="ts">` block with:
       - `const router = useRouter()`
       - `const navRoutes = computed(() => router.options.routes.filter((r) => r.meta?.nav === true).sort((a, b) => (a.meta?.navOrder ?? 999) - (b.meta?.navOrder ?? 999)))`
       - NO imports of `RouteRecordRaw` or vue-router (auto-imports handle `useRouter` and `computed`)

    2. Outer container: `<div class="flex h-screen overflow-hidden">`

    3. Sidebar `<aside class="flex flex-col w-64 h-screen bg-[var(--color-sidebar)] text-white shrink-0">`:
       - Header block: `<div class="px-6 py-6 border-b border-gray-700"><span class="text-lg font-semibold tracking-wide">Schoolday</span></div>` (literal text "Schoolday")
       - Nav list: `<ul class="flex-1 py-4 px-3 space-y-1">` with `<li v-for="route in navRoutes" :key="String(route.path)">`
       - Each `<NuxtLink :to="route.path" ...>`:
         - base class: `flex items-center gap-3 px-3 py-2 rounded-lg text-gray-400 hover:bg-gray-800 hover:text-white transition-colors text-sm font-medium`
         - `active-class="!bg-primary-600 !text-white"`
         - `exact-active-class="!bg-primary-600 !text-white"` (BOTH props — see Pitfalls 3 and 11; the `!` is required per § 6 to override base text-gray-400)
         - Icon: `<UIcon :name="(route.meta?.navIcon as string) ?? 'i-lucide-circle'" class="text-base shrink-0" />`
         - Label: `<span>{{ route.meta?.navLabel ?? route.name }}</span>`

    4. Main column `<div class="flex flex-col flex-1 overflow-hidden">`:
       - Header bar: `<header class="flex items-center h-14 bg-white border-b border-gray-200 pl-6 shrink-0"><span class="text-sm text-gray-700">Lakewood Unified School District</span></header>` (literal text "Lakewood Unified School District")
       - Main content: `<main class="flex-1 overflow-y-auto bg-gray-50"><slot /></main>` (slot INSIDE main — Pitfall 12)

    Use 2-space indentation per CLAUDE.md. No scoped styles. No CSS-in-JS. No `<RouterLink>` anywhere. No `bg-sidebar` utility — use `bg-[var(--color-sidebar)]` explicitly (Pitfall 6). Do NOT create a separate `app/components/SidebarNav.vue` (Pitfall 15) — inline everything in this file.

    After writing, run `npm run typecheck` and `npm run build` to confirm the layout compiles cleanly.
  </action>
  <verify>
    <automated>
      test -f app/layouts/default.vue &amp;&amp; \
      grep -q 'useRouter()' app/layouts/default.vue &amp;&amp; \
      grep -q 'router.options.routes' app/layouts/default.vue &amp;&amp; \
      grep -q "meta?.nav === true" app/layouts/default.vue &amp;&amp; \
      grep -q 'navOrder' app/layouts/default.vue &amp;&amp; \
      grep -q 'bg-\[var(--color-sidebar)\]' app/layouts/default.vue &amp;&amp; \
      grep -q '&lt;NuxtLink' app/layouts/default.vue &amp;&amp; \
      ! grep -q '&lt;RouterLink' app/layouts/default.vue &amp;&amp; \
      grep -q 'exact-active-class="!bg-primary-600 !text-white"' app/layouts/default.vue &amp;&amp; \
      grep -q 'active-class="!bg-primary-600 !text-white"' app/layouts/default.vue &amp;&amp; \
      grep -q '&lt;UIcon' app/layouts/default.vue &amp;&amp; \
      grep -q 'i-lucide-circle' app/layouts/default.vue &amp;&amp; \
      grep -q 'Schoolday' app/layouts/default.vue &amp;&amp; \
      grep -q 'Lakewood Unified School District' app/layouts/default.vue &amp;&amp; \
      grep -q '&lt;slot' app/layouts/default.vue &amp;&amp; \
      grep -q 'border-gray-700' app/layouts/default.vue &amp;&amp; \
      grep -q 'text-gray-400' app/layouts/default.vue &amp;&amp; \
      grep -q 'hover:bg-gray-800' app/layouts/default.vue &amp;&amp; \
      grep -q 'bg-gray-50' app/layouts/default.vue &amp;&amp; \
      npm run typecheck &amp;&amp; \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/layouts/default.vue` exits 0
    - `grep -q 'useRouter()' app/layouts/default.vue` matches
    - `grep -q 'router.options.routes' app/layouts/default.vue` matches (no hardcoded NAV_ROUTES — satisfies ROADMAP Success Criterion 4)
    - `grep -q 'meta?.nav === true' app/layouts/default.vue` matches (correct filter per Pitfall 8)
    - `grep -q 'navOrder' app/layouts/default.vue` matches (sort by navOrder)
    - `grep -q 'bg-\[var(--color-sidebar)\]' app/layouts/default.vue` matches (explicit arbitrary value per Pitfall 6 — NOT `bg-sidebar`)
    - `grep -q '<NuxtLink' app/layouts/default.vue` matches
    - `! grep -q '<RouterLink' app/layouts/default.vue` (no RouterLink regression — Pitfall: use NuxtLink only)
    - `grep -q 'exact-active-class="!bg-primary-600 !text-white"' app/layouts/default.vue` matches (verbatim active-state class string — Pitfalls 3, 11)
    - `grep -q 'active-class="!bg-primary-600 !text-white"' app/layouts/default.vue` matches (both props per § 3 Pattern 3)
    - `grep -q '<UIcon' app/layouts/default.vue` matches
    - `grep -q 'Schoolday' app/layouts/default.vue` matches (sidebar header text)
    - `grep -q 'Lakewood Unified School District' app/layouts/default.vue` matches (header bar text)
    - `grep -q '<slot' app/layouts/default.vue` matches (slot present)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0 — clean static build, no SSR errors, no macro errors
    - No new files appeared under `app/components/` (`! test -d app/components || test -z "$(ls -A app/components 2>/dev/null)"` — Pitfall 15 prevents accidental SidebarNav.vue)
  </acceptance_criteria>
  <done>app/layouts/default.vue renders the dark sidebar shell with auto-active NuxtLinks driven by router.options.routes; build is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Phase-gate dev-server smoke probe — verify all 5 routes SSR with sidebar + page-specific content</name>
  <files></files>
  <read_first>
    - .planning/phases/08-layout-routing/08-VALIDATION.md § Phase Requirements → Test Map (every probe row), § Sampling Rate (Phase gate criteria)
    - .planning/phases/08-layout-routing/08-RESEARCH.md § Validation Architecture (the 22-probe panel) and § 10 (compatibility check)
    - All files created in Tasks 1-3 (must be on disk before this task runs)
  </read_first>
  <action>
    No code changes in this task — pure verification. Execute the full grep-and-curl probe panel from 08-RESEARCH.md § Validation Architecture.

    1. Run `npm run typecheck` → expect exit 0.
    2. Run `npm run build` → expect exit 0; expect `.output/server/` and `.output/public/` to be produced (Nuxt 4 SSR build).
    3. Start the dev server in the background: `(npm run dev &)`; sleep 10 seconds to let it boot. Capture the bound port from the stdout — default is 3000, but Phase 7-02 noted port 3001 may be picked if 3000 is busy. Use `PORT` = whichever port the dev server logs.
    4. For each path in `/`, `/discovery`, `/dpa`, `/risk`, `/tags`:
       - `curl -s -o /dev/null -w "%{http_code}" http://localhost:$PORT$path` → expect `200`.
       - `curl -s http://localhost:$PORT$path` → save body and verify:
         - Body contains the page-specific `<h1>` text (Dashboard / Discovery / DPA / Risk Position / Tags).
         - Body contains the sidebar marker `Schoolday`.
         - Body contains the header bar marker `Lakewood Unified School District`.
         - Body contains ALL 5 nav labels (Dashboard, Discovery, DPA, Risk Position, Tags) — proves nav renders on every page.
    5. Negative probes (must NOT match):
       - `! find app/ src/ -maxdepth 4 \( -name 'router*.ts' -o -name 'router*.js' \) 2>/dev/null | grep -E 'router(/index)?\.(j|t)s$'` (no router config file anywhere)
       - `! grep -rE "createRouter|createWebHistory|createWebHashHistory" app/ nuxt.config.ts 2>/dev/null` (no router imports)
       - `! grep -q '"vue-router"' package.json` (no explicit vue-router dep — Phase 7 baseline)
       - `! grep -rE "pi pi-" app/ 2>/dev/null` (no PrimeIcons regression)
       - `! grep -q '<RouterLink' app/layouts/default.vue` (re-confirm)
    6. Kill the dev server cleanly: `kill %1` (or equivalent for the platform).

    If any probe fails, the executor MUST stop and either fix the offending file or report the failure — do not declare Phase 8 done with red probes.

    Out of scope (do NOT do): visual screenshot, interactive click-through (those are the manual UAT items in 08-VALIDATION.md § Manual-Only Verifications and belong to `/gsd:verify-work`).
  </action>
  <verify>
    <automated>
      npm run typecheck &amp;&amp; \
      npm run build &amp;&amp; \
      ( npm run dev &amp; ) &amp;&amp; sleep 10 &amp;&amp; \
      PORT=$(ss -tlnp 2&gt;/dev/null | grep -oE ':(3000|3001|3002)' | head -1 | tr -d ':') &amp;&amp; \
      PORT=${PORT:-3000} &amp;&amp; \
      for path in / /discovery /dpa /risk /tags; do \
        code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:$PORT$path") || exit 1; \
        [ "$code" = "200" ] || { echo "FAIL: $path returned $code"; exit 1; }; \
      done &amp;&amp; \
      curl -s "http://localhost:$PORT/" | grep -q '&lt;h1[^&gt;]*&gt;Dashboard&lt;/h1&gt;' &amp;&amp; \
      curl -s "http://localhost:$PORT/discovery" | grep -q '&lt;h1[^&gt;]*&gt;Discovery&lt;/h1&gt;' &amp;&amp; \
      curl -s "http://localhost:$PORT/dpa" | grep -q '&lt;h1[^&gt;]*&gt;DPA&lt;/h1&gt;' &amp;&amp; \
      curl -s "http://localhost:$PORT/risk" | grep -q '&lt;h1[^&gt;]*&gt;Risk Position&lt;/h1&gt;' &amp;&amp; \
      curl -s "http://localhost:$PORT/tags" | grep -q '&lt;h1[^&gt;]*&gt;Tags&lt;/h1&gt;' &amp;&amp; \
      for path in / /discovery /dpa /risk /tags; do \
        body=$(curl -s "http://localhost:$PORT$path") || exit 1; \
        echo "$body" | grep -q 'Schoolday' || { echo "FAIL: sidebar marker missing on $path"; exit 1; }; \
        echo "$body" | grep -q 'Lakewood Unified School District' || { echo "FAIL: header marker missing on $path"; exit 1; }; \
      done &amp;&amp; \
      count=$(curl -s "http://localhost:$PORT/" | grep -oE '&gt;(Dashboard|Discovery|DPA|Risk Position|Tags)&lt;' | sort -u | wc -l) &amp;&amp; \
      [ "$count" -eq 5 ] || { echo "FAIL: nav has $count of 5 expected labels"; exit 1; } &amp;&amp; \
      ! find app/ src/ -maxdepth 4 \( -name 'router*.ts' -o -name 'router*.js' \) 2&gt;/dev/null | grep -E 'router(/index)?\.(j|t)s$' &amp;&amp; \
      ! grep -rE "createRouter|createWebHistory|createWebHashHistory" app/ nuxt.config.ts 2&gt;/dev/null &amp;&amp; \
      ! grep -q '"vue-router"' package.json &amp;&amp; \
      ! grep -rE "pi pi-" app/ 2&gt;/dev/null &amp;&amp; \
      kill %1 2&gt;/dev/null; true
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run build` exits 0 (Nuxt 4 SSR build clean; .output/server/ and .output/public/ produced)
    - `curl http://localhost:$PORT/` returns HTTP 200; body contains `<h1[^>]*>Dashboard</h1>`
    - `curl http://localhost:$PORT/discovery` returns HTTP 200; body contains `<h1[^>]*>Discovery</h1>`
    - `curl http://localhost:$PORT/dpa` returns HTTP 200; body contains `<h1[^>]*>DPA</h1>`
    - `curl http://localhost:$PORT/risk` returns HTTP 200; body contains `<h1[^>]*>Risk Position</h1>`
    - `curl http://localhost:$PORT/tags` returns HTTP 200; body contains `<h1[^>]*>Tags</h1>`
    - All 5 route bodies contain `Schoolday` (sidebar persistent shell)
    - All 5 route bodies contain `Lakewood Unified School District` (header persistent shell)
    - Root `/` body contains ALL 5 nav labels (Dashboard, Discovery, DPA, Risk Position, Tags) → proves sidebar nav-list iteration works
    - Negative: no `router/index.ts`, no `router*.js` anywhere under `app/` or `src/`
    - Negative: no `createRouter` / `createWebHistory` / `createWebHashHistory` imports anywhere
    - Negative: no `vue-router` explicit dep in package.json
    - Negative: no `pi pi-*` PrimeIcons class regression in `app/`
    - Negative: no `<RouterLink` in `app/layouts/default.vue`
  </acceptance_criteria>
  <done>All 22 probes from 08-RESEARCH.md § Validation Architecture pass; Phase 8 deliverables are empirically verified on a live SSR dev server.</done>
</task>

</tasks>

<verification>
**Phase-level grep + curl panel** (executor MUST run this in order after Task 4):

1. **Static probes** (file existence + content; ~5s):
   - `test -f app/app.vue && test -f app/layouts/default.vue && test -f app/types/page-meta.d.ts`
   - `test -f app/pages/index.vue && test -f app/pages/discovery.vue && test -f app/pages/dpa.vue && test -f app/pages/risk.vue && test -f app/pages/tags.vue`
   - `grep -q '<NuxtLayout>' app/app.vue && grep -q '<NuxtPage' app/app.vue && grep -q '<UApp>' app/app.vue`
   - `grep -q "declare module '#app'" app/types/page-meta.d.ts && grep -q '^export {}' app/types/page-meta.d.ts`
   - For each page: `grep -q 'definePageMeta(' app/pages/$p.vue` AND `grep -q 'nav: true' app/pages/$p.vue`
   - `grep -q 'router.options.routes' app/layouts/default.vue && grep -q 'meta?.nav === true' app/layouts/default.vue`
   - `grep -q 'exact-active-class' app/layouts/default.vue && grep -q '<NuxtLink' app/layouts/default.vue`
   - `! grep -q '<RouterLink' app/layouts/default.vue`
   - `grep -q 'bg-\[var(--color-sidebar)\]' app/layouts/default.vue`
   - `grep -q 'Schoolday' app/layouts/default.vue && grep -q 'Lakewood Unified School District' app/layouts/default.vue`

2. **Build probes** (~35s):
   - `npm run typecheck` → exit 0
   - `npm run build` → exit 0, `.output/server/` and `.output/public/` exist

3. **Runtime probes** (~15s, requires dev server):
   - `npm run dev &` then `sleep 10`
   - For path in `/ /discovery /dpa /risk /tags`:
     - `curl -s -o /dev/null -w "%{http_code}" http://localhost:$PORT$path` → `200`
     - `curl -s http://localhost:$PORT$path | grep -q '<h1'` (page-specific content)
     - `curl -s http://localhost:$PORT$path | grep -q 'Schoolday'` (sidebar present)
     - `curl -s http://localhost:$PORT$path | grep -q 'Lakewood Unified School District'` (header present)
   - `curl -s http://localhost:$PORT/ | grep -oE '>(Dashboard|Discovery|DPA|Risk Position|Tags)<' | sort -u | wc -l` → `5`

4. **Negative probes** (defends against regressions):
   - `! find app/ src/ -name 'router*.ts' -o -name 'router*.js' 2>/dev/null | grep -E 'router(/index)?\.(j|t)s$'`
   - `! grep -rE "createRouter|createWebHistory|createWebHashHistory" app/ nuxt.config.ts 2>/dev/null`
   - `! grep -q '"vue-router"' package.json`
   - `! grep -rE "pi pi-" app/ 2>/dev/null`

**Manual UAT** (deferred to `/gsd:verify-work`, per 08-VALIDATION.md § Manual-Only Verifications):
- Open dev server in browser; visit `/discovery`; confirm Discovery nav item has brand-colored active-state background and contrast against the dark sidebar is acceptable.
- Click each of the 5 nav items in turn; confirm active highlight transfers (only the clicked item is highlighted; root `/` is NOT highlighted when on a sub-route — Pitfall 3 fix).
- Confirm no `[Vue warn]: Hydration` warnings in browser console on initial load of any route.
</verification>

<success_criteria>
- [ ] 7 new files created: `app/types/page-meta.d.ts`, `app/layouts/default.vue`, `app/pages/index.vue`, `app/pages/discovery.vue`, `app/pages/dpa.vue`, `app/pages/risk.vue`, `app/pages/tags.vue`
- [ ] 1 file modified: `app/app.vue` (Phase 7 stub replaced with `<UApp><NuxtLayout><NuxtPage /></NuxtLayout></UApp>`)
- [ ] All 4 ROADMAP Phase 8 Success Criteria verifiable via the verification panel above:
  1. `/`, `/discovery`, `/dpa`, `/risk`, `/tags` each return HTTP 200 with page-specific h1 SSR markup
  2. Sidebar (`Schoolday` + 5 NuxtLinks) and header (`Lakewood Unified School District`) appear in SSR HTML on every page; `exact-active-class` is set on every NuxtLink
  3. No `router/index.ts` or other router config file exists anywhere; no `createRouter`/`createWebHistory`/`createWebHashHistory` imports anywhere
  4. Sidebar reads nav data from `useRouter().options.routes` filtered by `meta.nav === true` and sorted by `meta.navOrder` — zero hardcoded route names in `app/layouts/default.vue`
- [ ] `npm run typecheck` exits 0
- [ ] `npm run build` exits 0
- [ ] NUXT-04 and LAYOUT-01 requirements closed (verifiable via grep probes mapping to requirement IDs)
- [ ] No npm installs occurred (`package.json` unchanged); no `nuxt.config.ts` edits (Phase 13 owns those)
- [ ] No new `app/components/*.vue` files (sidebar inlined in layout per Pitfall 15)
- [ ] No PrimeIcons (`pi pi-*`) regression in `app/`
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-routing/08-01-SUMMARY.md` following the standard summary template. Include:

- YAML frontmatter: `phase: 08-layout-routing`, `plan: 01`, `subsystem: routing-layout`, `tags: [nuxt-pages, nuxt-layouts, define-page-meta, nuxt-link, sidebar-nav, typescript]`, dependency `requires: [{ phase: 07-02, provides: ... }]` and `provides:` for what Phase 9+ consumes (file-based routing on 5 routes; persistent dark-sidebar shell; PageMeta type augmentation).
- **Tasks executed** — per-task summary including the verbatim list of files created and the actual port the dev server bound on.
- **Phase 8 ROADMAP Success Criteria — empirical evidence** table mapping each of the 4 criteria to the curl/grep output that proved it.
- **Phase-gate probe panel — N/N pass** with explicit listing of every probe from the verification block above.
- **Manual UAT (deferred to `/gsd:verify-work`)** — bullet list of the 3 manual checks (active highlight, click-through, no hydration warnings) for human follow-up.
- **Handoff to Phase 9** — note that the page stubs currently have no data; Phase 9 will replace each `<h1> + <p>` block with `useFetch`-driven content. The `app/types/page-meta.d.ts` augmentation is permanent and Phase 9+ should not redeclare those keys.
- **Known orphans** — `src/views/*.vue` (v0.5.0) and `src/components/layout/*.vue` are still on disk but outside Nuxt scan paths; they will be deleted by Phases 10-12 as those phases rewrite each view.
</output>
