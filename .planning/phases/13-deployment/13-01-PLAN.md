---
phase: 13-deployment
plan: 01
type: execute
wave: 1
subsystem: deploy-config
tags: [deployment, github-pages, config, adr]
depends_on: []
provides: [nuxt-config-baseURL, nuxt-config-nitro-preset, deploy-scripts, amplify-adr]
requires: []
affects: [nuxt.config.ts, package.json, .planning/adr/]
files_modified:
  - nuxt.config.ts
  - package.json
  - .planning/adr/AMPLIFY-GLIDEPATH.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]

must_haves:
  truths:
    - "nuxt.config.ts declares app.baseURL='/district-demo/' so generated HTML references /district-demo/_nuxt/ asset paths"
    - "nuxt.config.ts declares nitro.preset='github_pages' so nuxi generate auto-writes .nojekyll and pre-renders /404.html"
    - "nuxt.config.ts contains a glidepath comment naming aws-amplify and pointing to AMPLIFY-GLIDEPATH.md"
    - "package.json scripts.deploy chains nuxi generate, gh-pages -d .output/public, and --nojekyll flag"
    - "package.json scripts.postdeploy runs node scripts/smoke.mjs (npm lifecycle auto-fires after deploy)"
    - "AMPLIFY-GLIDEPATH.md exists with the verbatim 3-line Amplify migration switch and an amplify.yml skeleton"
  artifacts:
    - path: "nuxt.config.ts"
      provides: "Active GH Pages deployment config + Amplify glidepath comment"
      contains: "baseURL: '/district-demo/'"
    - path: "package.json"
      provides: "deploy + postdeploy scripts"
      contains: "\"deploy\":"
    - path: ".planning/adr/AMPLIFY-GLIDEPATH.md"
      provides: "Amplify migration ADR (why GH Pages now + exact 3-line switch)"
      min_lines: 40
  key_links:
    - from: "nuxt.config.ts comment block"
      to: ".planning/adr/AMPLIFY-GLIDEPATH.md"
      via: "explicit file path reference in source comment"
      pattern: "AMPLIFY-GLIDEPATH"
    - from: "package.json scripts.deploy"
      to: "nuxt.config.ts nitro.preset"
      via: "nuxi generate reads nuxt.config.ts → emits .output/public/ → gh-pages publishes"
      pattern: "nuxi generate && gh-pages -d .output/public --nojekyll"
    - from: "package.json scripts.postdeploy"
      to: "scripts/smoke.mjs (created in Plan 13-02)"
      via: "npm lifecycle hook"
      pattern: "node scripts/smoke.mjs"
---

<objective>
Author the three static-text deployment artifacts in parallel: `nuxt.config.ts` deployment block, `package.json` deploy/postdeploy scripts, and the Amplify migration ADR. These are pure file authorship — no build, no network, no runtime verification beyond grep. Each can be written independently because they share no in-file state (different files entirely).

Purpose: Lock in the deploy contract (DEPLOY-01 + DEPLOY-02) so Plan 13-02 can run `nuxi generate` against a configured project and Plan 13-03 can run `npm run deploy` against a wired-up script chain.

Output: 3 files written (2 edits + 1 new file), all grep-verifiable in ~1 second.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-deployment/13-CONTEXT.md
@.planning/phases/13-deployment/13-RESEARCH.md
@nuxt.config.ts
@package.json
</context>

<interfaces>
<!-- Concrete strings the executor MUST write verbatim. Sourced from RESEARCH.md OQ-9, OQ-2, OQ-4, OQ-7 (verified against installed package source code). -->

**nuxt.config.ts — final shape of the deployment block (replaces lines 30-42):**
```ts
  // === Deployment: GitHub Pages (v1.0.0) ===
  // To switch to AWS Amplify SSR: see .planning/adr/AMPLIFY-GLIDEPATH.md
  // 3-line switch: (1) remove app.baseURL, (2) set nitro.preset='aws-amplify', (3) add amplify.yml
  app: {
    baseURL: '/district-demo/',
  },
  nitro: {
    preset: 'github_pages',
  },
```

**package.json — scripts block (add 2 entries at the end of `scripts`):**
```json
"deploy": "nuxi generate && gh-pages -d .output/public --nojekyll",
"postdeploy": "node scripts/smoke.mjs"
```

**ADR template (RESEARCH.md §"AMPLIFY-GLIDEPATH.md — Skeleton" expanded):**
- Status / Date / Deciders header
- Context: why GH Pages now, what triggers the migration
- Decision: keep ssr:true + nuxi generate; structure config for 3-line switch
- Migration Steps: verbatim 3-line diff (remove baseURL, change preset, add amplify.yml)
- amplify.yml skeleton from RESEARCH.md OQ-7
- Consequences: hosting tradeoffs (free static vs Lambda SSR)
- Alternatives Considered: Vercel, Netlify, raw GH Actions
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace nuxt.config.ts deployment placeholder block with active GH Pages config + Amplify glidepath comment</name>
  <files>nuxt.config.ts</files>
  <read_first>
    - nuxt.config.ts (current state — lines 30-42 are the placeholder comment block to be replaced; lines 1-29 are existing modules/css/pinia/echarts config that MUST be preserved verbatim)
    - .planning/phases/13-deployment/13-RESEARCH.md §"OQ-9: nuxt.config.ts Lines 30-42 Placeholder Block" (lines 315-348 — shows exact before/after replacement block)
    - .planning/phases/13-deployment/13-RESEARCH.md §"Code Examples — nuxt.config.ts — Deployment Block (Final Shape)" (lines 544-587 — shows the canonical final file shape)
    - .planning/phases/13-deployment/13-CONTEXT.md §"Implementation Decisions" D-05, D-06, D-11, D-13 (lines 26-41 — locked decisions on baseURL value, preset value, comment terseness, ADR pointer)
  </read_first>
  <action>
    Replace `nuxt.config.ts` lines 30-42 (the placeholder comment block starting `// === Phase 13 deployment switches (do not enable in Phase 7) ===` and ending at the line before the closing `})`) with the active deployment config block.

    The final nuxt.config.ts file MUST contain — IN THIS EXACT ORDER inside defineNuxtConfig({...}):

    1. Preserve lines 1-28 verbatim (compatibilityDate, future, ssr:true, devtools, modules, css, pinia, piniaPluginPersistedstate, echarts — DO NOT touch these per D-02 carry-forward).
    2. Insert (replacing old lines 30-42) the following block exactly:

    ```ts
      // === Deployment: GitHub Pages (v1.0.0) ===
      // To switch to AWS Amplify SSR: see .planning/adr/AMPLIFY-GLIDEPATH.md
      // 3-line switch: (1) remove app.baseURL, (2) set nitro.preset='aws-amplify', (3) add amplify.yml
      app: {
        baseURL: '/district-demo/',
      },
      nitro: {
        preset: 'github_pages',
      },
    ```

    3. Close with `})` (line 43 of the new file).

    Implementation per locked decisions D-05 (app.baseURL='/district-demo/'), D-06 (nitro.preset='github_pages'), D-11 (short comment block — NO inline Amplify code, just the pointer to the ADR), D-12 (ADR path reference is `.planning/adr/AMPLIFY-GLIDEPATH.md` verbatim), D-13 (config is source of truth for the switch; ADR is source of truth for the why).

    DO NOT add `target: 'static'` — RESEARCH.md §Pitfall 4 confirms this is Nuxt 2 legacy and causes warnings in Nuxt 4.
    DO NOT set `ssr: false` — RESEARCH.md §Finding 4 + OQ-5 confirm `ssr: true` with `nuxi generate` is the correct pattern AND preserves the Amplify glidepath.
    DO NOT add any other nitro options (no `prerender`, no `routeRules`) — the `github_pages` preset already configures `crawlLinks: true` and the `/` + `/404.html` seed routes (RESEARCH.md OQ-1).
  </action>
  <verify>
    <automated>grep -E "baseURL.*'/district-demo/'" nuxt.config.ts && grep "preset: 'github_pages'" nuxt.config.ts && grep "aws-amplify" nuxt.config.ts && grep "AMPLIFY-GLIDEPATH" nuxt.config.ts && grep "ssr: true" nuxt.config.ts && ! grep "target: 'static'" nuxt.config.ts && ! grep "Phase 13 deployment switches" nuxt.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "baseURL: '/district-demo/'" nuxt.config.ts` returns 1
    - `grep -c "preset: 'github_pages'" nuxt.config.ts` returns 1
    - `grep -c "aws-amplify" nuxt.config.ts` returns 1 (only inside the comment line)
    - `grep -c "AMPLIFY-GLIDEPATH.md" nuxt.config.ts` returns 1
    - `grep -c "ssr: true" nuxt.config.ts` returns 1 (preserved from existing config)
    - `grep -c "target: 'static'" nuxt.config.ts` returns 0
    - `grep -c "Phase 13 deployment switches" nuxt.config.ts` returns 0 (old placeholder fully removed)
    - `grep -c "@nuxt/ui" nuxt.config.ts` returns 1 (modules block preserved)
    - `grep -c "nuxt-echarts" nuxt.config.ts` returns 1 (modules block preserved)
    - `grep -c "pinia-plugin-persistedstate/nuxt" nuxt.config.ts` returns 1 (modules block preserved)
    - `grep -c "Deployment: GitHub Pages (v1.0.0)" nuxt.config.ts` returns 1
    - Total file line count is within 5 lines of 45 (i.e. matches the OQ-9 final shape; 12 placeholder lines removed, 9 active-config lines added)
  </acceptance_criteria>
  <done>nuxt.config.ts has the active GH Pages deployment block with baseURL + nitro.preset, the 3-line Amplify glidepath comment pointing to the ADR, and the placeholder comment removed. All existing module/css/pinia/echarts config is untouched. ssr:true preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add deploy + postdeploy scripts to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state — scripts block at lines 6-13 to be extended; devDependencies already includes gh-pages@^6.3.0 at line 25 — no install needed)
    - .planning/phases/13-deployment/13-RESEARCH.md §"Pattern 2: npm Scripts Block" (lines 395-411 — shows the exact deploy + postdeploy strings)
    - .planning/phases/13-deployment/13-RESEARCH.md §"OQ-2: gh-pages Package + Nuxt 4 .output Gotchas" (lines 139-158 — confirms --nojekyll is mandatory; --dotfiles is rejected)
    - .planning/phases/13-deployment/13-RESEARCH.md §"Pitfall 1: _nuxt/ Assets 404 Without .nojekyll" (lines 473-481 — explains why --nojekyll is belt-and-suspenders alongside the preset hook)
    - .planning/phases/13-deployment/13-CONTEXT.md §"Implementation Decisions" D-02, D-08 (lines 19-33 — locked deploy script shape + postdeploy hook)
  </read_first>
  <action>
    Edit `package.json` to add two new entries to the `scripts` block. Preserve the existing 6 scripts (dev, build, generate, preview, typecheck, postinstall) verbatim.

    The final `scripts` block MUST be exactly:
    ```json
    "scripts": {
      "dev": "nuxi dev",
      "build": "nuxi build",
      "generate": "nuxi generate",
      "preview": "nuxi preview",
      "typecheck": "nuxi typecheck",
      "postinstall": "nuxi prepare",
      "deploy": "nuxi generate && gh-pages -d .output/public --nojekyll",
      "postdeploy": "node scripts/smoke.mjs"
    },
    ```

    Implementation per locked decisions D-02 (compound deploy: nuxi generate && gh-pages -d .output/public) and D-08 (postdeploy: node scripts/smoke.mjs).

    The `--nojekyll` flag IS MANDATORY per RESEARCH.md OQ-2 + Pitfall 1 — without it, gh-pages excludes the preset-written `.nojekyll` dotfile from the push, and GitHub Pages' Jekyll processor will hide the `_nuxt/` JS chunks. Per RESEARCH.md OQ-2, the gh-pages CLI native `--nojekyll` flag is preferred over `--dotfiles` (which would also include any other dotfiles that landed in `.output/public/`).

    DO NOT add `--branch gh-pages` (default is already `gh-pages` per RESEARCH.md OQ-2).
    DO NOT add `--dotfiles` — RESEARCH.md OQ-2 explicitly rejects it as too broad in scope.
    DO NOT add a new dependency — `gh-pages` is already at `package.json` line 25 (`"gh-pages": "^6.3.0"`).
    DO NOT modify the `dependencies` or `devDependencies` blocks.
    DO NOT reorder existing scripts — append `deploy` and `postdeploy` at the end of the scripts block (after `postinstall`).

    Validate the JSON parses correctly after editing (every other line is wired to nuxi; JSON syntax mistakes here cascade).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.deploy.includes('nuxi generate')) throw new Error('deploy missing nuxi generate'); if(!p.scripts.deploy.includes('gh-pages -d .output/public')) throw new Error('deploy missing gh-pages path'); if(!p.scripts.deploy.includes('--nojekyll')) throw new Error('deploy missing --nojekyll'); if(p.scripts.postdeploy !== 'node scripts/smoke.mjs') throw new Error('postdeploy wrong'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').scripts.deploy)"` outputs exactly `nuxi generate && gh-pages -d .output/public --nojekyll`
    - `node -e "console.log(require('./package.json').scripts.postdeploy)"` outputs exactly `node scripts/smoke.mjs`
    - `node -e "console.log(require('./package.json').scripts.dev)"` outputs `nuxi dev` (preserved)
    - `node -e "console.log(require('./package.json').scripts.generate)"` outputs `nuxi generate` (preserved)
    - `node -e "console.log(require('./package.json').devDependencies['gh-pages'])"` outputs `^6.3.0` (untouched)
    - `node -e "console.log(Object.keys(require('./package.json').scripts).length)"` outputs `8` (6 existing + 2 new)
    - `grep -c "nojekyll" package.json` returns 1
    - `grep -c "scripts/smoke.mjs" package.json` returns 1
    - `package.json` parses as valid JSON (above node commands fail noisily if not)
  </acceptance_criteria>
  <done>package.json has 8 scripts total. The deploy script chains `nuxi generate && gh-pages -d .output/public --nojekyll` and the postdeploy hook runs `node scripts/smoke.mjs`. Existing scripts and the gh-pages devDep are untouched. File is valid JSON.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author .planning/adr/AMPLIFY-GLIDEPATH.md migration record</name>
  <files>.planning/adr/AMPLIFY-GLIDEPATH.md</files>
  <read_first>
    - .planning/phases/13-deployment/13-RESEARCH.md §"OQ-7: AWS Amplify Nitro Preset for the ADR" (lines 253-300 — Amplify preset details, amplify.yml skeleton, exact 3-line switch wording)
    - .planning/phases/13-deployment/13-RESEARCH.md §"OQ-4: ADR Template Conventions" (lines 202-214 — standard Status/Context/Decision/Consequences format extended with Alternatives + Migration Steps)
    - .planning/phases/13-deployment/13-RESEARCH.md §"Code Examples — AMPLIFY-GLIDEPATH.md — Skeleton" (lines 661-699 — starting point template)
    - .planning/phases/13-deployment/13-CONTEXT.md §"Implementation Decisions" D-12, D-13 (lines 39-41 — ADR's purpose: expanded migration context, why GH Pages now vs Amplify later, env-var conventions, CI/CD differences, rollback plan, acceptance test)
    - .planning/REQUIREMENTS.md lines 37-38 (DEPLOY-02 contract — the 3-line switch is the gating contract this ADR documents)
    - nuxt.config.ts (post-Task-1 state — the ADR must reference the exact lines that change during the switch)
  </read_first>
  <action>
    Create the file `.planning/adr/AMPLIFY-GLIDEPATH.md` (directory was pre-created during planning).

    The ADR MUST include these sections IN ORDER, with at least these exact section headers (using `## `):

    1. **Title:** `# ADR: AWS Amplify SSR Glidepath`
    2. **Metadata block** (4 lines under title):
       - `**Status:** Accepted (implementation deferred to v1.1.0+)`
       - `**Date:** 2026-05-22`
       - `**Deciders:** Phase 13 planning`
       - `**Supersedes:** none`
    3. **`## Context`** — Explain (a) the District Demo Portal currently deploys as a static site to GitHub Pages for v1.0.0, (b) the long-term target is AWS Amplify SSR with branch-based envs and real API connections (v1.1.0+), (c) PROJECT.md historically referenced the future Amplify target, (d) Phase 13's contract DEPLOY-02 is that switching deploy targets MUST require only 3 changes with NO page-level code edits.
    4. **`## Decision`** — State: keep `ssr: true` + `nuxi generate` for v1.0.0; structure `nuxt.config.ts` so the GH Pages→Amplify switch is a 3-line diff. Reference the canonical config-source-of-truth pattern (D-13).
    5. **`## Migration Steps (3-line switch)`** — VERBATIM the following 3 steps:
       1. In `nuxt.config.ts`: remove `app: { baseURL: '/district-demo/' }`
       2. In `nuxt.config.ts`: change `nitro: { preset: 'github_pages' }` to `nitro: { preset: 'aws-amplify' }`
       3. Add `amplify.yml` to the repo root (skeleton below)

       Add a note: "The `ssr: true` line in `nuxt.config.ts` does not need to change — it is already true."
    6. **`## amplify.yml skeleton`** — Embed the exact yaml block from RESEARCH.md OQ-7 lines 266-284 (version 1, frontend.phases.preBuild with `nvm use 20` + `npm ci`, phases.build with `npm run build`, artifacts.baseDirectory: `.amplify-hosting`, artifacts.files: `'**/*'`, cache.paths: `node_modules/**/*`).
    7. **`## Consequences`** — Two-column comparison:
       - GH Pages (current): static HTML pre-rendered at build, no server, free hosting, ~1-2 min deploy, NO env vars needed, can NOT make runtime API calls (data baked at build time)
       - AWS Amplify (future): SSR on each request, Lambda-backed, supports real APIs, branch-based environments (dev/staging/prod), env vars via `NUXT_PUBLIC_*` (research §OQ-7), per-request Lambda invocation cost
    8. **`## Alternatives Considered`** — Brief paragraphs on:
       - Vercel: simpler deploy, but requires vendor account outside AWS ecosystem
       - Netlify: similar tradeoff to Vercel
       - GitHub Actions → node-server raw: more config, less automation than Amplify preset
       - Why Amplify wins: stays within AWS ecosystem, first-party Nitro preset handles output structure, supports the branch envs schoolday needs
    9. **`## Acceptance Test for the Future Migration`** — A checklist a future contributor can run:
       - [ ] After the 3-line switch, `npm run build` (NOT generate) exits 0
       - [ ] Output directory is `.amplify-hosting/` (NOT `.output/`); `compute/default/` and `static/` subdirs exist
       - [ ] `amplify.yml` parses (Amplify console validates on first deploy)
       - [ ] Live Amplify URL serves all 5 routes with SSR (curl shows fresh `__NUXT__` payload on each request, not a static dump)

    The ADR MUST grep-positive for: `aws-amplify`, `amplify.yml`, `github_pages`, `baseURL`, `ssr: true`, `.amplify-hosting`, `NUXT_PUBLIC_`.

    Per D-12 the ADR is the source of truth for *why*; per D-13 `nuxt.config.ts` is the source of truth for *the switch itself*. Do NOT copy/paste the entire `nuxt.config.ts` into the ADR — only the lines that change.

    Target length: 80-150 lines. Avoid prose padding — every paragraph should answer a future contributor's actual question.
  </action>
  <verify>
    <automated>test -f .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q "aws-amplify" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q "amplify.yml" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q "github_pages" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q "baseURL" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q "ssr: true" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q ".amplify-hosting" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -q "NUXT_PUBLIC" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -qE "^## Context" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -qE "^## Decision" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -qE "^## Migration Steps" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -qE "^## Consequences" .planning/adr/AMPLIFY-GLIDEPATH.md && grep -qE "^## Alternatives Considered" .planning/adr/AMPLIFY-GLIDEPATH.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/adr/AMPLIFY-GLIDEPATH.md` exists
    - `grep -c "aws-amplify" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥2 (mentioned in Decision, Migration Steps, and amplify.yml context)
    - `grep -c "amplify.yml" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥2
    - `grep -c "github_pages" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥1
    - `grep -c "baseURL" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥1
    - `grep -c "ssr: true" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥1
    - `grep -c ".amplify-hosting" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥1
    - `grep -c "NUXT_PUBLIC" .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥1
    - All required section headers present: `## Context`, `## Decision`, `## Migration Steps`, `## Consequences`, `## Alternatives Considered`
    - `wc -l .planning/adr/AMPLIFY-GLIDEPATH.md` returns ≥40
    - Status line `Status: Accepted` (or `Accepted (implementation deferred...)`) present
    - The 3 numbered migration steps appear as a numbered list (`1.`, `2.`, `3.`)
    - amplify.yml yaml block contains the literal strings `version: 1`, `frontend:`, `baseDirectory: .amplify-hosting`
  </acceptance_criteria>
  <done>ADR exists at `.planning/adr/AMPLIFY-GLIDEPATH.md` with Context, Decision, Migration Steps (3-line switch verbatim), amplify.yml skeleton, Consequences, Alternatives, and Acceptance Test sections. A future contributor can execute the migration from this file alone without re-deriving the plan.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, verify the plan-level invariants:

```bash
# All 3 files exist and grep-positive for their key strings
grep "baseURL: '/district-demo/'" nuxt.config.ts && \
grep "preset: 'github_pages'" nuxt.config.ts && \
grep "AMPLIFY-GLIDEPATH" nuxt.config.ts && \
grep "nuxi generate && gh-pages -d .output/public --nojekyll" package.json && \
grep "node scripts/smoke.mjs" package.json && \
test -f .planning/adr/AMPLIFY-GLIDEPATH.md && \
grep "aws-amplify" .planning/adr/AMPLIFY-GLIDEPATH.md && \
echo "PLAN 13-01 GREEN"
```

Validate JSON / TS still parse:
```bash
node -e "require('./package.json'); console.log('package.json OK')"
npm run typecheck  # nuxt.config.ts type-check (should already pass; baseURL + preset are typed)
```
</verification>

<success_criteria>
- nuxt.config.ts contains active app.baseURL + nitro.preset for github_pages AND the Amplify glidepath comment pointing to the ADR — placeholder block fully removed
- package.json scripts.deploy = `nuxi generate && gh-pages -d .output/public --nojekyll` and scripts.postdeploy = `node scripts/smoke.mjs` — existing 6 scripts preserved
- .planning/adr/AMPLIFY-GLIDEPATH.md exists with all required sections (Context, Decision, Migration Steps with verbatim 3-line switch, amplify.yml skeleton, Consequences, Alternatives, Acceptance Test)
- `npm run typecheck` passes (no regression introduced by config edits)
- All three files committed in a single coherent Phase 13 plan-01 commit
</success_criteria>

<output>
After completion, create `.planning/phases/13-deployment/13-01-SUMMARY.md` capturing:
- The exact final shape of nuxt.config.ts lines 30+ (the deployment block)
- The exact final shape of package.json scripts block
- ADR section list and line count
- Any deviations from the planned text (verbatim) and the reason
- Carry-forward notes for Plan 13-02 (smoke script will reference `Top 8 Vendors Needing Attention`, `Discovery`, `DPA`, `Risk Position`, `Tags` markers — all confirmed against actual page templates during planning)
</output>
